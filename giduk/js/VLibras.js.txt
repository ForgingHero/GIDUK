/*! Para obter informações sobre a licença, consulte vlibras-plugin.js.LICENSE */
protótipo) {\r\n obj[chave] = Emitter.prototype[chave];\r\n }\r\n return obj;\r\n}\r\n\r\n/**\r\ n * Ouça o `event` fornecido com `fn`.\r\n *\r\n * @param {String} event\r\n * @param {Function} fn\r\n * @return {Emitter }\r\n * @api public\r\n */\r\n\r\nEmitter.prototype.on =\r\nEmitter.prototype.addEventListener = function(event, fn){\r\n this. _callbacks = this._callbacks || {};\r\n (this._callbacks['$' + event] = this._callbacks['$' + event] || [])\r\n .push(fn);\r\n return this ;\r\n};\r\n\r\n/**\r\n * Adiciona um ouvinte `event` que será invocado uma única vez\r\n * e então removido automaticamente.\r\n * \r\n * @param {String} evento\r\n * @param {Função} fn\r\n * @return {Emissor}\r\n * @api public\r\n */\r\n \r\nEmitter.prototype.once = function(evento, fn){\r\n function on() {\r\n this.off(evento, on);\r\n fn. apply(this, argumentos);\r\n }\r\n\r\n on.fn = fn;\r\n this.on(evento, on);\r\n return this;\r\n };\r\n\r\n/**\r\n * Remove o callback fornecido para `event` ou todos os\r\n * callbacks registrados.\r\n *\r\n * @param {String } event\r\n * @param {Função} fn\r\n * @return {Emissor}\r\n * @api public\r\n */\r\n\r\nEmitter.prototype.off = \r\nEmitter.prototype.removeListener =\r\nEmitter.prototype.removeAllListeners =\r\nEmitter.prototype.removeEventListener = function(event, fn){\r\n this._callbacks = this._callbacks || {};\r\n\r\n // todos\r\n if (0 == argumentos.comprimento) {\r\n this._callbacks = {};\r\n return this;\r\n }\r\n\r\n // evento específico\r\n var callbacks = this._callbacks['$' + evento];\r\n if (!callbacks) retorna isso;\r\n\r\ n // remove todos os manipuladores\r\n if (1 == arguments.length) {\r\n delete this._callbacks['$' + event];\r\n return this;\r\n }\r\n\r\n // remove handler específico\r\n var cb;\r\n for (var i = 0; i < callbacks .length; i++) {\r\n cb = callbacks[i];\r\n if (cb === fn || cb.fn === fn) {\r\n callbacks.splice(i, 1 );\r\n break;\r\n }\r\n }\r\n return this;\r\n};\r\n\r\n/**\r\n * Emit `event ` com os argumentos fornecidos.\r\n *\r\n * @param {String} event\r\n * @param {Mixed} ...\r\n * @return {Emitter}\r\n * /\r\n\r\nEmitter.prototype.emit = function(evento){\r\n this._callbacks = this._callbacks || {};\r\n var args = [].slice.call(argumentos, 1)\r\n , callbacks = this._callbacks['$' + evento];\r\n\r\n if (callbacks ) {\r\n callbacks = callbacks.slice(0);\r\n for (var i = 0, len = callbacks.length; i < len; ++i) {\r\n callbacks[i]. apply(this, args);\r\n }\r\n }\r\n\r\n return this;\r\n}; \r\n\r\n/**\r\n * Retorna o array de callbacks para `event`.\r\n *\r\n * @param {String} event\r\n * @return {Array }\r\n * @api public\r\n */\r\n\r\nEmitter.prototype.listeners = function(event){\r\n this._callbacks = this._callbacks || {};\r\n retorna isso._callbacks['$' + evento] || [];\r\n};\r\n\r\n/**\r\n * Verifique se este emissor possui manipuladores `event`.\r\n *\r\n * @param {String} event\r\n * @return {Boolean}\r\n * @api public\r\n */\r\n\r\nEmitter.prototype.hasListeners = function(event){\r\n return !! this.listeners(event).length;\r\n};\r\n\n\n//# sourceURL=webpack://VLibras/../vlibras-player-webjs/node_modules/component-emitter/index .js?")},"../vlibras-player-webjs/node_modules/inherits/inherits_browser.js":function(module,exports){eval("if (typeof Object.create === 'function' ) {\n\t\t\treturn false;\n\t\t}\n\n\t\treturn true;\n\t} catch (err) {\n\t\t// Nós não Não espere que nenhum dos itens acima seja lançado, mas é melhor estar seguro.\n\t\treturn false;\n\t}\n}\n\nmodule.exports = shouldUseNative() ? Object.assign : função (destino, fonte) {\n\tvar de;\n\tvar to = toObject(destino);\n\tvar símbolos;\n\n\tfor (var s = 1; s < argumentos. length; s++) {\n\t\tfrom = Object(arguments[s]);\n\n\t\tfor (var key in from) {\n\t\t\tif (hasOwnProperty.call(from, chave)) {\n\t\t\t\tto[chave] = de[chave];\n\t\t\t}\n\t\t}\n\n\t\tif (getOwnPropertySymbols) {\n\t\t\tsymbols = getOwnPropertySymbols(from);\n\t\t\tfor (var i = 0; i < symbol.length; i++) {\n\t\t\t\tif (propIsEnumerable .call(de, símbolos[i])) {\n\t\t\t\t\tto[símbolos[i]] = de[símbolos[i]];\n\t\t\t\t} \n\t\t\t}\n\t\t}\n\t}\n\n\return to;\n};\n\n\n//# sourceURL=webpack://VLibras/ .. != root.location.protocol\n || !root.ActiveXObject)) {\n return new XMLHttpRequest;\n } else {\n try { return new ActiveXObject('Microsoft.XMLHTTP'); } catch(e) {}\n try { return new ActiveXObject('Msxml2.XMLHTTP.6.0'); } catch(e) {}\n try { return new ActiveXObject('Msxml2.XMLHTTP.3.0'); } catch(e) {}\n try { return new ActiveXObject('Msxml2.XMLHTTP'); } catch(e) {}\n }\n return false;\n};\n\n/**\n * Remove os espaços em branco iniciais e finais, adicionados para oferecer suporte ao IE.\n *\n * @param {String } s\n * @return {String}\n * @api private\n */\n\nvar trim = ''.trim\n ? função(ões) { return s.trim(); }\n : função(ões) { return s.replace(/(^\\s*|\\s*$)/g, ''); };\n\n/**\n * Serializa o `obj` fornecido. \n *\n * @param {String} str\n * @return {Object}\n * @api private\n */\n\nfunction parseHeader(str) {\n var linhas = str.split(/\ \r?\\n/);\n var campos = {};\n var índice;\n var linha;\n var campo;\n var val;\n\n linhas.pop(); // à direita CRLF\n\n for (var i = 0, len = linhas.comprimento; i < len; ++i) {\n linha = linhas[i];\n índice = linha.indexOf(':' );\n field = line.slice(0, index).toLowerCase();\n val = trim(line.slice(index + 1));\n fields[field] = val;\n }\n\ n campos de retorno;\n}\n\n/**\n * Verifique se `mime` é json ou tem +json sufixo de sintaxe estruturada.\n *\n * @param {String} mime\n * @return { Boolean}\n * @api private\n */\n\nfunction isJSON(mime) {\n return /[\\/+]json\\b/.test(mime);\n}\n\n/ **\n * Retorna o tipo mime para o dado `str`. \n * request.get('/', function(res){});\n *\n * O envio de dados pode ser encadeado:\n *\n * request\n * .post('/user')\ n * .send({ name: 'tj' })\n * .end(function(res){});\n *\n * Ou passado para `.send()`:\n *\n * request \n * .post('/user')\n * .send({ name: 'tj' }, function(res){});\n *\n * Ou passado para `.post()`:\ n *\n * request\n * .post('/user', { name: 'tj' })\n * .end(function(res){});\n *\n * Ou ainda reduzido a um chamada única para casos simples:\n *\n * request\n * .post('/user', { name: 'tj' }, function(res){});\n *\n * @param {XMLHTTPRequest } xhr\n * @param {Objeto} opções\n * @api private\n */\n\nfunção Resposta(req, opções) {\n opções = opções || {};\n this.req = req;\n this. xhr = this.req.xhr;\n // responseText é acessível somente se responseType for '' ou 'text' e em navegadores mais antigos\n this.text = ((this.req.method !='HEAD' && (this .xhr.responseType === '' || this.xhr.responseType === 'texto')) || typeof this.xhr.responseType === 'indefinido')\n ? this.xhr.responseText\n : null;\n this.statusText = this.req.xhr.statusText;\n this.setStatusProperties(this.xhr.status);\n this.header = this.headers = parseHeader(this .xhr.getAllResponseHeaders());\n // getAllResponseHeaders às vezes retorna falsamente \"\" para solicitações CORS, mas\n // getResponseHeader ainda funciona. portanto, obtemos o tipo de conteúdo mesmo se a obtenção\n // de outros cabeçalhos falhar.\n this.header['tipo de conteúdo'] = this.xhr.getResponseHeader('tipo de conteúdo');\n this. setHeaderProperties(this.header);\n this.body = this.req.method != 'HEAD'\n ? this.parseBody(this.text ? this.text : this.xhr.response)\n : null;\n}\n\n/**\n * Obtém o valor `campo` que não diferencia maiúsculas de minúsculas.\n *\n * @param {String} field\n * @return {String}\n * @api public\n */\n\nResponse.prototype.get = function(field){\n return this.header[field.toLowerCase( )];\n};\n\n/**\n * Definir propriedades relacionadas ao cabeçalho:\n *\n * - `.type` o tipo de conteúdo sem parâmetros\n *\n * Uma resposta de \"Conteúdo -Type: text/plain; charset=utf-8\"\n * fornecerá a você um `.type` de \"text/plain\".\n *\n * @param {Object} header\n * @api private\n */\n\nResponse.prototype.setHeaderProperties = function(header){\n // tipo de conteúdo\n var ct = this.header['tipo de conteúdo'] || '';\n this.type = type(ct); \n\n // params\n var obj = params(ct);\n for (var key in obj) this[key] = obj[key];\n};\n\n/**\n * Analisa o corpo fornecido `str`.\n *\n * Usado para análise automática de corpos. Analisadores\n * são definidos no objeto `superagent.parse`.\n *\n * @param {String} str\n * @return {Mixed}\n * @api private\n */\n\nResponse. protótipo.parseBody = function(str){\n var parse = request.parse[this.type];\n if (!parse && isJSON(this.type)) {\n parse = request.parse['application/json '];\n }\n return parse && str && (str.length || str instanceof Object)\n ? parse(str)\n : null;\n};\n\n/**\n * Defina sinalizadores como `.ok` com base em `status`.\n *\n * Por exemplo, uma resposta 2xx fornecerá você um `.ok` de __true__\n * enquanto 5xx será __false__ e `.error` será __true__. Os arquivos\n * `.clientError` e `. serverError` também estão disponíveis para serem mais\n * específicos, e `.statusType` é a classe de erro que varia de 1..5\n * às vezes útil para mapear cores de resposta, etc.\n *\n * \"açúcar\ " propriedades também são definidas para casos comuns. Atualmente fornecendo:\n *\n * - .noContent\n * - .badRequest\n * - .unauthorized\n * - .notAcceptable\n * - .notFound\n *\n * @param {Number} status\n * @api private\n */\n\nResponse.prototype.setStatusProperties = function(status){\n // lida com o bug do IE9: http://stackoverflow.com/questions/10046972/msie-returns-status-code- of-1223-for-ajax-request\n if (status === 1223) {\n status = 204;\n }\n\n var type = status / 100 | 0;\n\n // status / classe\n this.status = this.statusCode = status;\n this.statusType = tipo;\n\n // básico\n this.info = 1 == tipo;\ n this.ok = 2 == tipo;\n this. clientError = 4 == type;\n this.serverError = 5 == type;\n this.error = (4 == type || 5 == type)\n ? this.toError()\n : false;\n\n // sugar\n this.accepted = 202 == status;\n this.noContent = 204 == status;\n this.badRequest = 400 == status; \n this.unauthorized = 401 == status;\n this.notAcceptable = 406 == status;\n this.notFound = 404 == status;\n this.forbidden = 403 == status;\n};\n \n/**\n * Retorna um `Error` representativo desta resposta.\n *\n * @return {Error}\n * @api public\n */\n\nResponse.prototype.toError = function( ){\n var req = this.req;\n var method = req.method;\n var url = req.url;\n\n var msg = 'cannot ' + method + ' ' + url + ' (' + this.status + ')';\n var err = new Error(msg);\n err.status = this.status;\n err.method = método;\n err.url = url;\n\n return erro;\n}; \n\n/**\n * Expor `Response`.\n */\n\nrequest.Response = Response;\n\n/**\n * Inicializar um novo `Request` com o `método` fornecido e `url`.\n *\n * @param {String} method\n * @param {String} url\n * @api public\n */\n\nfunction Request(method, url) {\n var self = esta;\n esta._consulta = esta._consulta || [];\n this.method = método;\n this.url = url;\n this.header = {}; // preserva maiúsculas e minúsculas do nome do cabeçalho\n this._header = {}; // força nomes de cabeçalho para letras minúsculas\n this.on('end', function(){\n var err = null;\n var res = null;\n\n try {\n res = new Response(self) ;\n } catch(e) {\n err = new Error('O analisador não pode analisar a resposta');\n err.parse = true;\n err.original = e;\n // problema #675 : retorna a resposta bruta se a análise da resposta falhar\n err.rawResponse = self.xhr && self.xhr. respostaTexto? self.xhr.responseText : null;\n // problema nº 876: retorna o código de status http se a análise da resposta falhar\n err.statusCode = self.xhr && self.xhr.status ? self.xhr.status : null;\n return self.callback(err);\n }\n\n self.emit('resposta', res);\n\n if (err) {\n return self. callback(err, res);\n }\n\n if (res.status >= 200 && res.status < 300) {\n return self.callback(err, res);\n }\n\n var new_err = new Error(res.statusText || 'Resposta HTTP sem sucesso');\n new_err.original = err;\n new_err.response = res;\n new_err.status = res.status;\n\n self.callback (new_err, res);\n });\n}\n\n/**\n * Mixin `Emitter` e `requestBase`.\n */\n\nEmitter(Request.prototype);\nfor ( var chave em requestBase) {\n Request.prototype[chave] = requestBase[chave]; type = function(type){\n this.set('Content-Type', request.types[type] || type);\n return this;\n};\n\n/**\n * Set tipo de resposta para `val`. Os tipos de resposta válidos atualmente são 'blob' e \n * 'arraybuffer'.\n *\n * Exemplos:\n *\n * req.get('/')\n * .responseType('blob')\n * .end(callback);\n *\n * @param {String} val\n * @return {Request} para encadeamento\n * @api public\n */\n\nRequest.prototype.responseType = function(val ){\n this._responseType = val;\n return this;\n};\n\n/**\n * Defina Accept como `type`, mapeando valores de `request.types`.\n *\n * Exemplos:\n *\n * superagent.types.json = 'application/json';\n *\n * request.get('/agent')\n * .accept('json')\n * . end(retorno de chamada);\n *\n * request.get('/agente')\n * .accept(' content-type'];\n\n // merge\n if (obj && isObject(this._data)) {\n for (var key in data) {\n this._data[key] = data[key] ;\n }\n } else if ('string' == tipo de dado) {\n if (!type) this.type('form');\n type = this._header['content-type']; \n if ('aplicativo/x-www-form-urlencoded' == tipo) {\n this._data = this._data\n ? this._data + '&' + dados\n : dados;\n } else {\n this._data = (this._data || '') + dados;\n }\n } else {\n this._data = data;\n }\n\n if (!obj || isHost(data)) return this;\n if (!type) this.type('json');\n return this;\n};\ n\n/**\n * @obsoleto\n */\nResponse.prototype.parse = function serialize(fn){\n if (root.console) {\n console.warn(\" O método parse() do lado do cliente foi renomeado para serialize(). Este método não é compatível com o superagente v2.0\");\n }\n this.serialize(fn);\n return this;\n};\n\nResponse.prototype.serialize = function serialize(fn){ \n this._parser = fn;\n return this;\n};\n\n/**\n * Invoque o callback com `err` e `res`\n * e manipule a verificação de aridade.\n *\ n * @param {Erro} err\n * @param {Resposta} res\n * @api private\n */\n\nRequest.prototype.callback = function(err, res){\n var fn = this. _callback;\n this.clearTimeout();\n fn(err, res);\n};\n\n/**\n * Invoca retorno de chamada com erro de domínio x.\n *\n * @api private \n */\n\nRequest.prototype.crossDomainError = function(){\n var err = new Error('A solicitação foi encerrada\\nPossíveis causas: a rede está off-line, a origem não é permitida por Access-Control-Allow -Origin, a página está sendo descarregada, etc. \n\n/**\n * Inicia a solicitação, invocando o callback `fn(res)`\n * com uma instância de `Response`.\n *\n * @param {Function} fn\n * @return {Request } para encadeamento\n * @api public\n */\n\nRequest.prototype.end = function(fn){\n var self = this;\n var xhr = this.xhr = request.getXHR();\ n var query = this._query.join('&');\n var timeout = this._timeout;\n var data = this._formData || this._data;\n\n // armazena callback\n this._callback = fn || noop;\n\n // mudança de estado\n xhr.onreadystatechange = function(){\n if (4 != xhr.readyState) return;\n\n // No IE9, lê qualquer propriedade (por exemplo, status) off de um XHR abortado resultará\n // no erro \"Não foi possível concluir a operação devido ao erro c00c023f\"\n var status;\n try { status = xhr.status } catch(e) { status = 0 ; }\n\n if (0 == estado) {\n if (self. com/IE/feedback/details/837245/xmlhttprequest-upload-throws-invalid-argument-when-used-from-web-worker-context\n }\n\n // timeout\n if (timeout && !this. _timer) {\n this._timer = setTimeout(function(){\n self.timedout = true;\n self.abort();\n }, timeout);\n }\n\n // querystring\n if (consulta) {\n consulta = request.serializeObject(consulta);\n this.url += ~this.url.indexOf('?')\n ? '&' + consulta\n : '?' + query;\n }\n\n // inicia solicitação\n if (this.username && this.password) {\n xhr.open(this.method, this.url, true, this.username, this.password );\n } else {\n xhr.open(this.method, this.url, true);\n }\n\n // CORS\n if (this._withCredentials) xhr.withCredentials = true;\n \n // corpo\n if ('GET' != this.method && 'HEAD' != this. method && 'string' != typeof data && !isHost(data)) {\n // serialize coisas\n var contentType = this._header['content-type'];\n var serialize = this._parser || request.serialize[contentType ? contentType.split(';')[0] : ''];\n if (!serialize && isJSON(contentType)) serialize = request.serialize['application/json'];\n if (serialize) data = serialize (dados);\n }\n\n // definir campos de cabeçalho\n para (campo var in this.header) {\n if (null == this.header[campo]) continue;\n xhr.setRequestHeader( field, this.header[field]);\n }\n\n if (this._responseType) {\n xhr.responseType = this._responseType;\n }\n\n // enviar coisas\n this.emit ('request', this);\n\n // IE11 xhr.send(undefined) envia 'indefinido' \n\n/**\n * Suporte de promessa falsa\n *\n * @param {Função} cumprir\n * @param {Função} rejeitar\n * @return {Pedido}\n */\n\nexports .then = função then(preencher, rejeitar) {\n retornar isso.end(função(err, res) {\n err ? rejeitar(err) : cumprir(res);\n });\n}\n\ n/**\n * Permitir extensão\n */\n\nexports.use = function use(fn) {\n fn(this);\n return this;\n}\n\n\n/* *\n * Obtém o cabeçalho `field` do pedido.\n * Não diferencia maiúsculas de minúsculas.\n *\n * @param {String} field\n * @return {String}\n * @api public\n */\n \nexports.get = function(field){\n return this._header[field.toLowerCase()];\n};\n\n/**\n * Obtém o valor `field` do cabeçalho que não diferencia maiúsculas de minúsculas.\n * Esta é uma API interna obsoleta. Em vez disso, use `.get(field)`. \n strArray[0] = first + strArray[0];\n }\n\n // Deve haver duas ou três barras no protocolo do arquivo, duas barras em qualquer outra coisa.\n if (strArray[0]. match(/^file:\\/\\/\\//)) {\n strArray[0] = strArray[0].replace(/^([^/:]+):\\/*/, '$1:///');\n } else {\n strArray[0] = strArray[0].replace(/^([^/:]+):\\/*/, '$1:// ');\n }\n\n for (var i = 0; i < strArray.length; i++) {\n var component = strArray[i];\n\n if (typeof component !== 'string' ) {\n throw new TypeError('Url deve ser uma string. Received ' + component);\n }\n\n if (component === '') { continue; }\n\n if (i > 0) {\n // Removendo as barras iniciais para cada componente, exceto o primeiro.\n component = component.replace(/^[\\/]+/, '');\ n }\n if (i < strArray.length - 1) {\n // Removendo as barras finais para cada componente, exceto o último.\n component = component.replace(/[\\/]+$/, '');\n } else {\ n // Para o último componente, combinaremos várias barras em uma única.\n component = component.replace(/[\\/]+$/, '/');\n }\n\n resultArray.push (componente);\n\n }\n\n var str = resultArray.join('/');\n // Cada componente de entrada agora é separado por uma única barra, exceto a possível primeira parte simples do protocolo.\n\ n // remove a barra antes dos parâmetros ou hash\n str = str.replace(/\\/(\\?|&|#[^!])/g, '$1');\n\n // substitui ? em parâmetros com &\n var parts = str.split('?');\n str = parts.shift() + (parts.length > 0 ? '?': '') + parts.join('&' ); translate = function (text) {\n this.emit('translate:start');\n\n if (this.loaded) {\n this.stop();\n }\n\n this.text = text;\n\n this.translator.translate(text, (gloss, error) => {\n if (error) {\n this.play(text.toUpperCase());\n this.emit('error ', 'translation_error');\n return;\n }\n \n // console.log('Resposta do tradutor:', gloss);\n this.play(gloss, true);\n this.emit( 'translate:end');\n });\n};\n\nPlayer.prototype.play = function (glosa, fromTranslation = false) {\n\n this.translated = fromTranslation;\n this.gloss = glosa || this.gloss;\n \n if (this.gloss !== indefinido && this.loaded) {\n this.changeStatus(STATUSES.preparing);\n this.playerManager.play(this.gloss);\n } \n};\n\nPlayer.prototype.playWellcome = function () {\n this. onPlayingStateChange = function (\n isPlaying, isPaused, isPlayingIntervalAnimation, isLoading, isRepeatable) {\n PlayerManagerAdapter.instance.emit(\n 'stateChange', toBoolean(isPlaying), toBoolean(isPaused), toBoolean(isLoading)\n);\ n};\n\nwindow.CounterGloss = função (contador, glosaLenght) {\n PlayerManagerAdapter.instance.emit(\n 'CounterGloss', contador, glosaLenght\n );\n};\n\nwindow.FinishWelcome = função (bool) {\n PlayerManagerAdapter.instance.emit(\n 'FinishWelcome', bool\n );\n};\n\nfunction toInt(boolean) {\n return !boolean ? 0 : 1;\n};\n\nfunction toBoolean(bool) {\n return bool != 'False';\n};\n\nmodule.exports = PlayerManagerAdapter;\n\n\n//# sourceURL =webpack://VLibras/../vlibras-player-webjs/src/PlayerManagerAdapter.js?")},"../vlibras-player-webjs/src/config. \\n direita: 0;\\n superior: 0;\\n esquerda: 0;\\n inferior: 0; }\\n\\n.noUi-handle {\\n posição: relativa;\\n z-index: 1; }\\n\\n.noUi-stacking .noUi-handle {\\n z-index: 10; }\\n\\n.noUi-state-tap .noUi-origin {\\n -webkit-transition: esquerda .3s,topo .3s;\\n transição: esquerda .3s,topo .3s; }\\n\\n.noUi-state-drag * {\\n cursor: herdar !important; }\\n\\n.noUi-base, .noUi-handle {\\n -webkit-transform: translate3d(0, 0, 0);\\n transform: translate3d(0, 0, 0); }\\n\\n.noUi-horizontal {\\n altura: 18px; }\\n\\n.noUi-horizontal .noUi-handle {\\n largura: 34px;\\n altura: 28px;\\n esquerda: -17px;\\n superior: -6px; }\\n\\n.noUi-vertical {\\n largura: 18px; }\\n\\n.noUi-vertical .noUi-handle {\\n largura: 28px;\\n altura: 34px;\\n esquerda: -6px;\\n superior: -17px; }\\n\\n.noUi-background {\\n background: #FAFAFA;\\n box-shadow: insert 0 1px 1px #f0f0f0; }\\n\\n. noUi-connect {\\n plano de fundo: #3FB8AF;\\n caixa-sombra: inserção 0 0 3px rgba(51, 51, 51, 0,45);\\n -webkit-transition: plano de fundo 450ms;\\n transição: fundo 450ms; }\\n\\n.noUi-origin {\\n border-radius: 2px; }\\n\\n.noUi-target {\\n border-radius: 4px;\\n border: 1px solid #D3D3D3;\\n box-shadow: inset 0 1px 1px #F0F0F0,0 3px 6px -5px #BBB; }\\n\\n.noUi-target.noUi-connect {\\n caixa-sombra: inserir 0 0 3px rgba(51, 51, 51, 0,45), 0 3px 6px -5px #BBB; }\\n\\n.noUi-arrastável {\\n cursor: w-resize; }\\n\\n.noUi-vertical .noUi-arrastável {\\n cursor: n-redimensionar; }\\n\\n.noUi-handle {\\n border: 1px solid #D9D9D9;\\n border-radius: 3px;\\n background: #FFF;\\n cursor: default;\\n box -sombra: inserir 0 0 1px #FFF,inserir 0 1px 7px #EBEBEB,0 3px 6px -3px #BBB; }\\n\\n.noUi-active {\\n box-shadow: insert 0 0 1px #FFF,inset 0 1px 7px #DDD, 0 3px 6px -3px #BBB; }\\n\\n.noUi-handle:depois, .noUi-handle:antes {\\n conteúdo: \\"\\";\\n exibição: bloco;\\n posição: absoluto;\\n altura: 14px;\\n largura: 1px;\\n fundo: #E8E7E6;\\n esquerda: 14px;\\n topo: 6px; }\\n\\n.noUi-handle:after {\\n left: 17px; }\\n\\n.noUi-vertical .noUi-handle:depois, .noUi-vertical .noUi-handle:antes {\\n largura: 14px;\\n altura: 1px;\\n esquerda: 6px; \\n topo: 14px; }\\n\\n.noUi-vertical .noUi-handle:after {\\n top: 17px; }\\n\\n[desativado] .noUi-connect, [desativado].noUi-connect {\\n plano de fundo: #B8B8B8; }\\n\\n[desativado] .noUi-handle, [desativado].noUi-origin {\\n cursor: não permitido; }\\n\\n.noUi-pips, .noUi-pips * {\\n -moz-box-sizing: border-box;\\n box-sizing: border-box; }\\n\\n.noUi-pips {\\n posição: absoluta;\\n cor: #999; }\\n\\n.noUi-value {\\n posição: absoluta; \\n text-align: center; }\\n\\n.noUi-value-sub {\\n color: #ccc;\\n font-size: 10px; }\\n\\n.noUi-marcador {\\n posição: absoluto;\\n fundo: #CCC; }\\n\\n.noUi-marker-large, .noUi-marker-sub {\\n background: #AAA; }\\n\\n.noUi-pips-horizontal {\\n padding: 10px 0;\\n height: 80px;\\n top: 100%;\\n left: 0;\\n width: 100 %; }\\n\\n.noUi-value-horizontal {\\n -webkit-transform: translate3d(-50%, 50%, 0);\\n transform: translate3d(-50%, 50%, 0) ; }\\n\\n.noUi-marker-horizontal.noUi-marker {\\n margin-left: -1px;\\n width: 2px;\\n height: 5px; }\\n\\n.noUi-marker-horizontal.noUi-marker-sub {\\n height: 10px; }\\n\\n.noUi-marker-horizontal.noUi-marker-large {\\n height: 15px; }\\n\\n.noUi-pips-vertical {\\n padding: 0 10px;\\n height: 100%;\\n top: 0;\\n left: 100%; }\\n\\n.noUi-value-vertical {\\n -webkit-transform: translate3d(0, -50%, 0);\\n transform: translate3d(0, -50%, 0);\\n padding-left: 25px; }\\n\\n.noUi-marker-vertical.noUi-marker {\\n width: 5px;\\n height: 2px;\\n margin-top: -1px; }\\n\\n.noUi-marker-vertical.noUi-marker-sub {\\n width: 10px; }\\n\\n.noUi-marker-vertical.noUi-marker-large {\\n largura: 15px; }\\n\\n.noUi-tooltip {\\n display: block;\\n position: absolute;\\n border: 1px solid #D9D9D9;\\n border-radius: 3px;\\n background: #fff;\\n padding: 5px;\\n text-align: center; }\\n\\n.noUi-horizontal .noUi-handle-lower .noUi-tooltip {\\n top: -32px; }\\n\\n.noUi-horizontal .noUi-handle-upper .noUi-tooltip {\\n bottom: -32px; }\\n\\n.noUi-vertical .noUi-handle-lower .noUi-tooltip {\\n esquerda: 120%; }\\n\\n.noUi-vertical .noUi-handle-upper .noUi-tooltip {\\n direita: 120%; }\\n", ""]);\n\n\n//# sourceURL=webpack: 50%;\\n transform: translate(-50%, -50%);\\n font-size: 15px !important;\\n font-family: \'Open Sans\', sans-serif; }\\n", ""]);\n\n\n//# sourceURL=webpack://VLibras/./plugin/components/Box/box.scss?./node_modules/css-loader/dist/ cjs.js!./node_modules/sass-loader/dist/cjs.js')},"./node_modules/css-loader/dist/cjs.js!./node_modules/sass-loader/dist/cjs.js! ./plugin/components/ChangeAvatar/change-avatar.scss":function(module,exports,__webpack_require__){eval('exports = module.exports = __webpack_require__(/*! ../../../node_modules/css -loader/dist/runtime/api.js */ "./node_modules/css-loader/dist/runtime/api.js")(false);\n// Module\nexports.push([module.i, " [vp] [vp-change-avatar] {\\n display: flex;\\n flex-direction: linha;\\n justify-content: flex-end;\\n position: absolute; \\n índice z: 2;\\n visibilidade: oculto;\\n largura máxima: 100%;\\n opacidade: 1;\\n transição: largura máxima 0s, visibilidade 0s, opacidade 0,4s; \\n esquerda: 13px;\\n superior: 50px; }\\n [vp] [vp-alterar-avatar] .vp-button-change-avatar {\\n exibição: flex;\\n justificar-conteúdo: centro;\\n alinhar-itens: centro;\\ n z-index: 1;\\n cursor: ponteiro;\\n background: rgba(0, 0, 0, 0.3);\\n border-radius: 50px;\\n width: 32px;\\n height : 32px;\\n box-shadow: 1px 1px 2px 1px rgba(0, 0, 0, 0,45); }\\n [vp] [vp-change-avatar] .vp-button-change-avatar .change-avatar-icon {\\n width: 22px;\\n margin-right: 1px;\\n filter: brilho(0) inverter(1);\\n display: nenhum; }\\n [vp] [vp-change-avatar] .vp-button-change-avatar .active {\\n display: block; }\\n [vp] [vp-change-avatar].vp-disabled {\\n visibilidade: visível; \\n -moz-align-items: center;\\n align-items: center;\\n width: calc(100% - 10px);\\n height: 60px;\\n background-size: 100% 100%;\\n fundo: branco;\\n largura: 100%; }\\n [vp] .vpw-controls .vpw-controls-legendas, [vp] .vpw-controls .vpw-controls-slider {\\n margem: 0 9px;\\n cursor: ponteiro; }\\n [vp] .vpw-controls .vpw-controls-play .vpw-component-play {\\n exibição: bloco;\\n margem: 0 8.5px;\\n cursor: ponteiro; }\\n [vp] .vpw-controls .vpw-controls-play .vpw-component-pause {\\n display: nenhum; }\\n [vp] .vpw-controls .vpw-controls-play .vpw-component-restart {\\n display: nenhum; }\\n [vp] .vpw-controls.vpw-playing .vpw-controls-play .vpw-component-play {\\n display: nenhum; }\\n [vp] .vpw-controls.vpw-playing .vpw-controls-play .vpw-component-pause {\\n display: block;\\n margin: 0 11px;\\n cursor: ponteiro; }\\n [vp] .vpw-controls.vpw-playing .vpw-controls-play .vpw-component-restart {\\n display: nenhum; }\\n [vp] .vpw-controls.vpw-stopped .vpw-controls-play .vpw-component-play {\\n display: nenhum; }\\n [vp] .vpw-controls.vpw-stopped .vpw-controls-play .vpw-component-pause {\\n display: nenhum; }\\n [vp] .vpw-controls.vpw-stopped .vpw-controls-play .vpw-component-restart {\\n display: block;\\n margin: 0 2px;\\n cursor: pointer; }\\n [vp] .vpw-controls.vpw-subtitles .vpw-controls-subtitles .vpw-legenda-habilitar {\\n display: block; }\\n [vp] .vpw-controls.vpw-subtitles .vpw-controls-subtitles .vpw-legenda-desabilitar {\\n display: nenhum; }\\n [vp] .vpw-controls .vpw-controls-subtitles .vpw-legenda-habilitar {\\n display: nenhum; }\\n [vp] .vpw-controls .vpw-controls-legendas . vpw-legenda-desabilitar {\\n display: block; }\\n\\n@-webkit-keyframes spin {\\n 0% ​​{\\n -webkit-transform: rotate(0deg);\\n /* Chrome, Opera 15+, Safari 3.1+ */\ \n -ms-transform: rotate(0deg);\\n /* IE 9 */\\n transform: rotate(0deg);\\n /* Firefox 16+, IE 10+, Opera */ }\\ n 100% {\\n -webkit-transform: rotate(360deg);\\n /* Chrome, Opera 15+, Safari 3.1+ */\\n -ms-transform: rotate(360deg);\\n / * IE 9 */\\n transform: rotate(360deg);\\n /* Firefox 16+, IE 10+, Opera */ } }\\n\\n@keyframes spin {\\n 0% ​​{\ \n -webkit-transform: rotate(0deg);\\n /* Chrome, Opera 15+, Safari 3.1+ */\\n -ms-transform: rotate(0deg);\\n /* IE 9 */ \\n transform: rotate(0deg);\\n /* Firefox 16+, IE 10+, Opera */ }\\n 100% {\\n -webkit-transform: rotate(360deg);\\n / * Chrome, Opera 15+, Safari 3. 1+ */\\n -ms-transform: rotate(360deg);\\n /* IE 9 */\\n transform: rotate(360deg);\\n /* Firefox 16+, IE 10+, Opera */ } }\\n\\n[vp] .vpw-speed-default {\\n borda: 1px cinza sólido;\\n raio da borda: 3px 3px 3px 3px;\\n preenchimento: 3px 4px;\ \n altura: 100%;\\n cor: cinza;\\n tamanho da fonte: 15px;\\n cursor: ponteiro;\\n família da fonte: \'Open Sans\', sans-serif; }\\n\\n[vp] .vpw-img-default {\\n display: nenhum;\\n padding: 2px;\\n padding-left: 2.2px;\\n cursor: pointer; }\\n\\n[vp] .vpw-border-default {\\n display: nenhum;\\n border: 1px solid #999999;\\n border-radius: 0px 0px 3px 3px; }\\n\\n[vp] .vpw-elem-speed {\\n exibição: nenhuma;\\n posição: absoluta;\\n fundo: 22px;\\n cursor: ponteiro; }\\n\\n[vp] .vpw-block-speed {\\n tipo de estilo de lista: nenhum;\\n cor de fundo: #003F86;\\n cor: branco;\\n fonte- tamanho: 10px; \\n largura: 25,5px;\\n alinhamento de texto: centro; }\\n\\n[vp] .vpw-block-speed:hover {\\n cor: #67C8D5; }\\n\\n[vp] .vpw-controls-speed {\\n posição: relativa;\\n -webkit-touch-callout: nenhum;\\n -webkit-user-select: nenhum;\\n -khtml-user-select: nenhum;\\n -moz-user-select: nenhum;\\n -ms-user-select: nenhum;\\n user-select : nenhum; }\\n\\n[vp] .vpw-controls-speed-number {\\n largura: 25,5px;\\n padding-left: 0px;\\n margin-top: 0px;\\n margin- inferior: 0px;\\n font-family: \'Open Sans\', sans-serif; }\\n\\n[vp] .vpw-block-speed-3 {\\n border-radius: 5px 5px 0px 0px; }\\n\\n[vp] .vpw-controls-slider {\\n -webkit-appearance: nenhum;\\n largura: 50%;\\n margem: 2,5px 0;\\n altura: 7px ;\\n cor de fundo: transparente; }\\n [vp] .vpw-controls-slider .vpw-slider {\\n largura: 100%;\\n altura: 7px; }\\n [vp] .vpw-controls-slider .vpw-slider.noUi-target {\\n box-shadow: nenhum;\\n border: 0; }\\n [vp] .vpw-controls-slider .vpw-slider.noUi-connect {\\n background-color: #003F86; }\\n [vp] .vpw-controls-slider . vpw-slider .noUi-background {\\n background-color: #B8B8B8;\\n box-shadow: nenhum; }\\n [vp] .vpw-controls-slider .vpw-slider .noUi-origin {\\n border-radius: 1px; }\\n [vp] .vpw-controls-slider .vpw-slider .noUi-handle {\\n largura: 0px;\\n altura: 0px;\\n esquerda: -8px;\\n superior: - 8px;\\n raio da borda: 50%; }\\n [vp] .vpw-controls-slider .vpw-slider .noUi-handle:depois, [vp] .vpw-controls-slider .vpw-slider .noUi-handle:antes {\\n display: nenhum ; }\\n", ""]);\n\n\n//# sourceURL=webpack://VLibras/./plugin/components/Controls/controls.scss?./node_modules/css-loader/dist/ cjs.js!./node_modules/sass-loader/dist/cjs.js')},"./node_modules/css-loader/dist/cjs.js!./node_modules/sass-loader/dist/cjs.js! ./plugin/components/Dictionary/dictionary.scss":function(module,exports, \\n largura: 80%;\\n esquerda: 50%;\\n transformação: translateX(-50%);\\n margem superior: 20px; }\\n [vp] .vpw-dictionary .vpw-panel .vpw-search entrada {\\n posição: absoluta;\\n largura: 100%;\\n esquerda: 50%;\\n transformação: translateX (-50%);\\n preenchimento: 10px 10px 10px 10px;\\n raio da borda: 6px;\\n borda: 1px sólido #DDD;\\n contorno: nenhum; }\\n [vp] .vpw-dictionary .vpw-panel .vpw-search input:focus {\\n border: 1px solid #00ddf9; }\\n [vp] .vpw-dictionary .vpw-panel .vpw-search .vpw-icon {\\n exibição: bloco;\\n posição: relativa; }\\n [vp] .vpw-dictionary .vpw-panel .vpw-search .vpw-icon img {\\n posição: absoluta;\\n altura: 24px;\\n largura: 24px;\\n topo : 6px;\\n direita: 7px; }\\n [vp] . vpw-dictionary .vpw-dict-container {\\n posição: absoluta;\\n largura: 80%;\\n altura: 68%;\\n esquerda: 50%;\\n transformação: translateX(-50 %); }\\n [vp] .vpw-dictionary .vpw-dict-container ul {\\n height: 100%;\\n margin: 0;\\n padding: 0;\\n overflow-y: scroll; \\n tipo de estilo de lista: nenhum; }\\n [vp] .vpw-dictionary .vpw-dict-container ul::-webkit-scrollbar {\\n largura: 8px; }\\n [vp] .vpw-dictionary .vpw-dict-container ul::-webkit-scrollbar-track {\\n background-color: #DDD; }\\n [vp] .vpw-dictionary .vpw-dict-container ul::-webkit-scrollbar-thumb {\\n background-color: #BCBCBC; }\\n [vp] .vpw-dictionary .vpw-dict-container ul li {\\n tamanho da fonte: 14px;\\n cursor: ponteiro; }\\n [vp] .vpw-dictionary .vpw-dict-container ul li: hover {\\n cor de fundo: #EAEAEA;\\n -webkit-transition: cor de fundo 0,3s;\\n transição: cor de fundo 0,4s; }\\n [vp] .vpw-dictionary .vpw-dict-container ul li.margin {\\n height: 114px; }\\n [vp] .vpw-dictionary .vpw-dict-container ul li:not(.margin) {\\n padding: 9px 10px 9px 10px; }\\n", ""]);\n\n\n//# sourceURL=webpack://VLibras/./plugin/components/Dictionary/dictionary.scss?./node_modules/css-loader/dist/ cjs.js!./node_modules/sass-loader/dist/cjs.js')},"./node_modules/css-loader/dist/cjs.js!./node_modules/sass-loader/dist/cjs.js! ./plugin/components/InfoScreen/info-screen.scss":function(module,exports,__webpack_require__){eval('exports = module.exports = __webpack_require__(/*! ../../../node_modules/css -loader/dist/runtime/api. vpw-info-screen #vpw-info-tabset {\\n posição: relativa;\\n superior: 0;\\n flex-grow: 4;\\n display: -webkit-flex;\\n display: flex;\\n flex-direction: coluna;\\n -webkit-flex-direction: coluna;\\n preenchimento: 0;\\n altura: 100%;\\n largura: 100%;\\n alinhamento -itens: centro;\\n -webkit-align-itens: centro; }\\n [vp] .vpw-info-screen #vpw-info-main {\\n largura máxima: 600px;\\n família da fonte: \\"Open Sans\\", sans-serif;\ \n flex-direction: coluna;\\n -webkit-flex-direction: coluna; }\\n [vp] .vpw-info-screen #vpw-info-main.active {\\n exibição: flex;\\n alinhar conteúdo: centro;\\n justificar conteúdo: centro;\\n itens de alinhamento: centro; }\\n [vp] .vpw-info-screen #vpw-info-logo img {\\n altura: 90px; }\\n [vp] .vpw-info-screen #vpw-info-realizadores {\\n altura: 100%;\\n min-width: 230px;\\n largura: 50%;\\n flex-direction: coluna;\\n align-items: esticar;\\n -webkit-flex-direction: coluna;\\n -webkit-align-items: esticar; }\\n [vp] .vpw-info-screen #vpw-info-realizadores #vpw-realiz {\\n font-size: 20px; }\\n [vp] .vpw-info-screen #vpw-info-realizadores.active {\\n exibição: flex;\\n alinhar-conteúdo: centro;\\n justificar-conteúdo: centro;\\n itens de alinhamento: centro; }\\n [vp] .vpw-info-screen #vpw-info-realizadores .vpw-siz {\\n max-width: 55% !important; }\\n [vp] .vpw-info-screen #vpw-info-realizadores .vpw-logo {\\n margin-top: -2px;\\n margin: 0.5em 0.35em;\\n min-width : 10%;\\n largura máxima: 29%;\\n altura máxima: 15vh; }\\n [vp] .vpw-info-screen #vpw-info-realizadores .vpw-logo .vpw-short-ver {\\n max-width: nenhum;\\n min-height: 16px; }\\n [vp] .vpw-info-screen #vpw-info-realizadores .vpw-logo-group {\\n display: flex;\\n padding: 0.5% 2%;\\n flex-direction: linha;\\n flex-wrap: embrulhar;\\n alinhar itens: centralizar;\\n justificar conteúdo: espaço ao redor;\\n -webkit-flex-direction: linha;\\n -webkit-flex -wrap: wrap;\\n -webkit-align-items: centro;\\n -webkit-justify-content: espaço ao redor;\\n altura da linha: 1,1; }\\n [vp] .vpw-info-screen #vpw-info-realizadores .vpw-logo-group .p-separator {\\n margem superior: 10px;\\n margem inferior: 10px;\\ n largura: 118px; }\\n [vp] .vpw-info-screen #vpw-info-realizadores .vpw-logo-group .format-text {\\n margem: 0px; }\\n [vp] .vpw-info-screen .vpw-info-tab {\\n flex-grow: 3;\\n -webkit-flex-grow: 3;\\n display: nenhum; }\\n [vp] .vpw-info-screen .vpw-info-tab . vpw-info-tab-label {\\n posição: absoluta;\\n superior: 0%;\\n tamanho da fonte: 16px;\\n família da fonte: \\"Open Sans\\", sans- serifa; }\\n [vp] .vpw-info-screen .vpw-info-tab .vpw-info-tab-label-two {\\n tamanho da fonte: 16px;\\n família da fonte: \\"Abrir Sans\\", sans-serif;\\n margin-top: 10px; }\\n [vp] .vpw-info-screen .vpw-info-tab.active {\\n exibição: bloco; }\\n [vp] .vpw-info-screen #vpw-info-meta p {\\n margem: 0,5em 0 0; }\\n [vp] .vpw-info-screen #vpw-info-meta a {\\n decoração de texto: nenhum; }\\n [vp] .vpw-info-screen .vpw-info-bullet {\\n background-size: 15px 15px;\\n display: inline-block;\\n width: 15px;\\n height : 15px;\\n conteúdo: \\"\\"; }\\n [vp] .vpw-info-screen #vpw-info-tab-bullets {\\n largura: 100%;\\n altura: auto;\\n posição: absoluta; \\n bottom: 0%;\\n text-align: center;\\n flex-grow: 1;\\n -webkit-flex-grow: 1;\\n cursor: ponteiro; }\\n [vp] .vpw-info-screen .vpw-close-btn {\\n posição: absoluta;\\n superior: 8px;\\n direita: 6px;\\n z-index: 4; }\\n [vp] .vpw-info-screen .vpw-close-btn img.icon {\\n largura: 34px;\\n altura: 34px; }\\n\\n@media somente tela e (-moz-min-device-pixel-ratio: 2), somente tela e (-o-min-device-pixel-ratio: 2 / 1), somente tela e (-webkit-min-device-pixel-ratio: 2), somente tela e (min-device-pixel-ratio: 2) {\\n [vp] #vpw-info-realizadores .vpw-logo {\\n margem: 0,5em 0,35em;\\n largura mínima: 10%;\\n largura máxima: 50%;\\n altura máxima: 18vh; } }\\n\\n@tela somente mídia e (altura máxima: 360px) {\\n [vp] #vpw-info-principal {\\n tamanho da fonte: 0,84em; } }\\n", ""]);\n\n\n//# sourceURL=webpack: 0px 2px 5px #888888;\\n -webkit-box-shadow: 0px 2px 5px #888888;\\n box-shadow: 0px 2px 5px #888888;\\n text-align: center;\\n font-family : \'Abrir Sans\', sem serifa; }\\n [vp] .vpw-message-box.active {\\n topo: 0;\\n opacidade: 1; }\\n [vp] .vpw-message-box.info {\\n cor de fundo: #003F86;\\n cor: branco; }\\n [vp] .vpw-message-box.warning {\\n cor de fundo: #003F86;\\n cor: branco; }\\n [vp] .vpw-message-box.success {\\n cor de fundo: #003F86;\\n cor: branco; }\\n [vp] .vpw-message-box.default {\\n cor de fundo: #003F86;\\n cor: branco; }\\n [vp] .vpw-message-box a {\\n cor: herdar; }\\n", ""]);\n\n\n//# sourceURL=webpack://VLibras/./plugin/components/MessageBox/message-box.scss?./node_modules/css-loader/ dist/cjs.js!./node_modules/sass-loader/dist/cjs.js' translateY(69px);\\n transição: max-height 0s, transform 0.4s linear 0.05s; }\\n [vp] [vp-rate-box] .vp-container .vp-text {\\n text-align: center;\\n font-size: 14px;\\n font-weight: bold; \\n cor: #003f86; }\\n [vp] [vp-rate-box] .vp-container .vp-buttons {\\n display: flex;\\n flex-direction: row;\\n justify-content: center;\\ n margem superior: 10px; }\\n [vp] [vp-rate-box] .vp-container .vp-buttons .vp-button {\\n display: flex;\\n flex-direction: linha;\\n align-items: centro;\\n tamanho da fonte: 13px;\\n espessura da fonte: negrito;\\n cor: #545454;\\n cursor: ponteiro; }\\n [vp] [vp-rate-box] .vp-container .vp-buttons .vp-button:not(:last-child) {\\n margin-right: 22px; }\\n [vp] [vp-rate-box] .vp-container .vp-buttons . botão vp img {\\n altura: 22px;\\n margem direita: 6px; }\\n [vp] [vp-rate-box].vp-enabled {\\n z-index: 4; }\\n [vp] [vp-rate-box].vp-enabled .vp-container {\\n max-height: 100%;\\n transform: translateY(0);\\ntransition: transform 0.4 s, altura máxima 0s linear 0,4s; }\\n", ""]);\n\n\n//# sourceURL=webpack://VLibras/./plugin/components/RateBox/rate-box.scss?./node_modules/css-loader/ dist/cjs.js!./node_modules/sass-loader/dist/cjs.js')},"./node_modules/css-loader/dist/cjs.js!./node_modules/sass-loader/dist/cjs. js!./plugin/components/RateButton/rate-button.scss":function(module,exports,__webpack_require__){eval('exports = module.exports = __webpack_require__(/*! ../../../node_modules /css-loader/dist/runtime/api.js */ "./node_modules/css-loader/dist/runtime/api.js" \\n topo: 0;\\n esquerda: 0;\\n largura: 100%;\\n altura: 100%;\\n índice z: 6;\\n cor de fundo: #003f86;\ \n cor: branco; }\\n [vp] .vpw-settings .vpw-content .vpw-flag {\\n posição: absoluta;\\n altura: 24px;\\n margem superior: -12px;\\n margem esquerda : 4px;\\n borda: nenhuma;\\n raio da borda: 5px; }\\n [vp] .vpw-settings .vpw-content > .vpw-bar {\\n posição: absoluta;\\n largura: 100%;\\n altura: 54px; }\\n [vp] .vpw-settings .vpw-content > .vpw-bar .vpw-title {\\n posição: absoluto;\\n topo: 16px;\\n esquerda: 50px;\\n cor : #6481b8;\\n tamanho da fonte: 12px;\\n espessura da fonte: negrito; }\\n [vp] .vpw-settings .vpw-content > ul {\\n list-style-type: nenhum;\\n margin: 0;\\n padding: 0 11%;\\n font- família: \\" seta-esquerda-opac {\\n margem superior: -13px;\\n largura: 25px;\\n flutuante: direita; }\\n [vp] .vpw-settings .vpw-content > ul li:hover {\\n color: #67c8d5; }\\n [vp] .vpw-settings .vpw-content > ul .vpw-position {\\n display: nenhum; }\\n [vp] .vpw-settings .vpw-content > ul .vpw-opacity {\\n display: nenhum; }\\n [vp] .vpw-settings .vpw-content > ul .vpw-localism {\\n padding: 24px 0; }\\n [vp] .vpw-settings .vpw-content > ul .vpw-localism .vpw-flag {\\n direita: 27px;\\n altura: 24px; }\\n [vp] .vpw-settings .vpw-content > ul .vpw-localism .vpw-abbrev {\\n posição: absoluta;\\n direita: 0px;\\n margem superior: -9px; \\n tamanho da fonte: 14px;\\n espessura da fonte: negrito; }\\n [vp] .vpw-configurações . vpw-content > ul .vpw-localism .vpw-arrow {\\n posição: absoluta;\\n direita: 16px;\\n altura: 13px;\\n margem superior: -6px; }\\n [vp] .vpw-settings .vpw-content > .vpw-localism {\\n posição: absoluto;\\n topo: 0;\\n esquerda: 0;\\n largura: 100%; \\n height: 100%;\\n z-index: 7;\\n background-color: white;\\n border: 1px solid rgba(0, 0, 0, 0.2);\\n border-radius : 2 px; }\\n [vp] .vpw-settings .vpw-content > .vpw-localism:not(.active) {\\n visibilidade: oculto;\\n opacidade: 0;\\n -webkit-transition: visibilidade 0s, opacidade 0,3s;\\n transição: visibilidade 0s, opacidade 0,3s; }\\n [vp] .vpw-settings .vpw-content > .vpw-localism.active {\\n visibilidade: visível;\\n opacidade: 1;\\n -webkit-transition: visibilidade 0s, opacidade 0,3s;\\n transição: visibilidade 0s, opacidade 0,3s; }\\n [vp] .vpw-settings .vpw-content > .vpw-localism .vpw-name {\\n posição: absoluta;\\n margem superior: -7px;\\n margem esquerda: 9px ;\\n tamanho da fonte: 14px;\\n espessura da fonte: negrito; }\\n [vp] .vpw-settings .vpw-content > .vpw-localism .vpw-national {\\n posição: relativa;\\n largura: calc(33,33%);\\n margem superior: 6px;\\n preenchimento superior: 16px;\\n exibição: flex;\\n direção flexível: linha;\\n conteúdo justificado: centro;\\n itens de alinhamento: centro; }\\n [vp] .vpw-configurações .vpw-conteúdo > .vpw-localismo .vpw-nacional .vpw-nome {\\n cor: preto;\\n posição: relativa; }\\n [vp] .vpw-settings .vpw-content > .vpw-localism .vpw-national .vpw-flag {\\n posição: relativo; }\\n [vp] .vpw-settings .vpw-content > .vpw-localism .vpw-regions {\\n posição: relativa;\\n largura: 100%;\\n altura: 100%;\\ n box-sizing: content-box;\\n padding: 0px 8px 0px 8px;\\n left: 0;\\n top: 50%;\\n transform: translateY(-50%); }\\n [vp] .vpw-settings .vpw-content > .vpw-localism .vpw-regions > .vpw-container-regions {\\n exibição: bloco em linha;\\n posição: relativa;\\ n largura: 33,33%;\\n margem inferior: 1px;\\n preenchimento: 6px 0;\\n margem superior: 1%;\\n cursor: ponteiro;\\n opacidade: 0,5;\\n -webkit-transition: opacidade 0,3s;\\n transição: opacidade 0,3s; }\\n [vp] .vpw-settings .vpw-content > .vpw-localism .vpw-regions > .vpw-container-regions.selected, [vp] .vpw-settings .vpw-content > .vpw-localism .vpw-regions > .vpw-container-regions:hover {\\n opacity: 1;\\n -webkit-transition: opacity 0.3s;\ \n transição: opacidade 0,3s; }\\n [vp] .vpw-settings .vpw-content > .vpw-localism .vpw-regions > .vpw-container-regions .vpw-cont {\\n display: flex;\\n flex-direction: linha;\\n justificar-conteúdo: centro;\\n alinhar-itens: centro; }\\n [vp] .vpw-settings .vpw-content > .vpw-localism .vpw-regions > .vpw-container-regions > .vpw-cont .vpw-flag {\\n position: relativo;\\ n margem: 0; }\\n [vp] .vpw-settings .vpw-content > .vpw-localism .vpw-regions > .vpw-container-regions > .vpw-cont .vpw-name {\\n position: parente;\\ n margem: 0 10px 0;\\n cor: cinza; }\\n [vp] .vpw-settings .vpw-content .vpw-vlibras-logo {\\n posição: absoluta;\\n esquerda: 50%;\\n transformação: translateX(-50%);\ \n bottom: 0;\\n z-index: 6; }\\n [vp] .vpw-settings .vpw-content .vpw-vlibras-logo .vpw-vlibras-span {\\n left: 50%;\\n transform: translateX(-50%);\\ n posição: absoluto;\\n inferior: 15%;\\n alinhamento de itens: centro;\\n tamanho da fonte: 14px; }\\n [vp] .vpw-settings .vpw-content .vpw-vlibras-logo .vpw-logo {\\n largura: 100%; }\\n [vp] .vpw-settings > .vpw-wall {\\n posição: absoluta;\\n superior: 0;\\n esquerda: 0;\\n inferior: 0;\\n direita: 0;\\n índice z: 5; }\\n [vp] .vpw-settings.active {\\n display: bloco; }\\n [vp] .vpw-settings.active .vpw-content {\\n esquerda: 0; \\n -webkit-transition: esquerda 0,3s;\\n transição: esquerda 0,3s; }\\n [vp] .vpw-settings.active .vpw-wall {\\n visibilidade: visível; }\\n [vp] .vpw-settings:not(.active) {\\n display: nenhum; }\\n [vp] .vpw-settings:not(.active) .vpw-content {\\n esquerda: 0;\\n visibilidade: oculto;\\n -webkit-transition: esquerda 0,3s;\\ n transição: esquerda 0,3s; }\\n [vp] .vpw-settings:not(.active) .vpw-wall {\\n visibilidade: oculto; }\\n [vp] .vpw-settings .vpw-clicável {\\n cursor: ponteiro; }\\n [vp] .vpw-settings .vpw-clickable:não(:hover) {\\n cor de fundo: herdada;\\n -webkit-transition: 0,12s cor de fundo;\\n transição: cor de fundo de 0,12s; }\\n [vp] .vpw-settings .vpw-clickable:hover {\\n -webkit-transition: 0,2s cor de fundo;\\n transição: 0,2s cor de fundo; }\\n\\n[vp] .vpw-controls-dictionary:not(.vpw-loading-dictionary):before {\\n content: \\"Dicionário\\";\\n font-size : 14px;\\n posição: absoluta;\\n margem superior: -11px; }\\n\\n[vp] .vpw-controls-dictionary.vpw-loading-dictionary {\\n min-width: 22px;\\n min-height: 22px;\\n margin-top: -17px ;\\n posição: absoluta;\\n borda esquerda: 3px solid rgba(255, 255, 255, 0.2);\\n border-top: 3px solid rgba(255, 255, 255, 0.6);\\ n borda inferior: 3px branco sólido;\\n borda direita: 3px sólida rgba(255, 255, 255, 0,6);\\n estilo da borda: inserção;\\n raio da borda: 50%;\\ n -webkit-animation: gira 1,5s linear infinito;\\n animação: gira 0,8s linear infinito; }\\n\\n/*@media (min-width: 600px) {\\n .vpw-settings {\\n &.active {\\n visibilidade: visível;\\n esquerda: 0;\\ n\\n -webkit-transition: visibilidade 0s, esquerda 0,3s; 0;\\n fundo: 0;\\n cor de fundo: #2196F3; }\\n [vp] .vpw-slider-check:antes de {\\n posição: absoluta;\\n conteúdo: \\"\\";\\n altura: 26px;\\n largura: 26px;\ \n esquerda: 4px;\\n inferior: 4px;\\n cor de fundo: branco; }\\n [vp] input:checked + .vpw-slider-check {\\n background-color: #2196F3; }\\n [vp] input:focus + .vpw-slider-check {\\n box-shadow: 0 0 1px #ccc; }\\n [vp] input:checked + .vpw-slider-check:before {\\n -webkit-transform: translateX(26px);\\n -ms-transform: translateX(26px);\\n transform : traduzirX(26px); }\\n [vp] .vpw-slider-check.vpw-round {\\n border-radius: 34px; }\\n [vp] .vpw-slider-check.vpw-round:before {\\n border-radius: 50%;\\n top: -3px; }\\n", ""]);\n\n\n//# sourceURL=webpack://VLibras/./plugin/components/Settings/switch.scss?. vpw-settings-btn-menu.active {\\n display: bloco; }\\n [vp] .vpw-settings-btn .vpw-settings-btn-close {\\n exibição: nenhuma;\\n posição: absoluta;\\n topo: 50%;\\n transformação: translateY (-50%);\\n direita: 5px; }\\n", ""]);\n\n\n//# sourceURL=webpack://VLibras/./plugin/components/SettingsBtn/settings-btn.scss?./node_modules/css-loader/ dist/cjs.js!./node_modules/sass-loader/dist/cjs.js')},"./node_modules/css-loader/dist/cjs.js!./node_modules/sass-loader/dist/cjs. js!./plugin/components/SettingsCloseBtn/settings-close-btn.scss":function(module,exports,__webpack_require__){eval('exports = module.exports = __webpack_require__(/*! ../../.. /node_modules/css-loader/dist/runtime/api.js */ "./node_modules/css-loader/dist/runtime/api.js")(false);\n// Module\nexports.push([module .i, "[vp] . vpw-btn-close {\\n posição: absoluto;\\n superior: 0;\\n largura: 100%;\\n altura: 100%;\\n exibição: nenhum;\\n z-index: 7;\\n cursor: ponteiro; }\\n [vp] .vpw-btn-close.active {\\n exibição: bloco; }\\n [vp] .vpw-btn-close .vpw-img {\\n posição: absoluta;\\n superior: 50%;\\n transformação: translateY(-50%);\\n largura: 37,92px;\\n altura: 37,92px;\\n direita: 5px; }\\n", ""]);\n\n\n//# sourceURL=webpack://VLibras/./plugin/components/SettingsCloseBtn/settings-close-btn.scss?./node_modules/css- loader/dist/cjs.js!./node_modules/sass-loader/dist/cjs.js')},"./node_modules/css-loader/dist/cjs.js!./node_modules/sass-loader/dist/ cjs.js!./plugin/components/SuggestionButton/suggestion-button.scss":function(module,exports,__webpack_require__){eval('exports = module.exports = __webpack_require__(/*! ../../.. vp-bounderies .vp-container .vp-open-screen-button {\\n margem inferior: 16px; }\\n [vp] [vp-suggestion-button] .vp-button-container {\\n position: absolute;\\n display: flex;\\n flex-direction: coluna;\\n align-items : centro;\\n inferior: 0;\\n largura: 100%;\\n padding-bottom: 10px;\\n z-index: 1;\\n opacidade: 0;\\n transição: opacidade 0,4 s; }\\n [vp] [vp-suggestion-button] .vp-button-container .vp-close-button {\\n position: absolute;\\n bottom: -13px; }\\n [vp] [vp-suggestion-button].vp-enabled {\\n z-index: 4; }\\n [vp] [vp-suggestion-button].vp-enabled .vp-container {\\n transform: translateY(0);\\ntransition-delay: 0s; }\\n [vp] [vp-suggestion-button].vp-enabled .vp-button-container {\\n opacidade: 1;\\n transição-atraso: 0,4s; }\\n", ""]); \\n esquerda: 100%;\\n transição: esquerda 0,4s de avanço suave;\\n /* Track */\\n /* Handle */\\n /* Handle on hover */ }\\n [vp] [vp-suggestion-screen] ::-webkit-scrollbar {\\n largura: 10px; }\\n [vp] [vp-suggestion-screen] ::-webkit-scrollbar-track {\\n background: #d1d1d1f1 !important; }\\n [vp] [vp-suggestion-screen] ::-webkit-scrollbar-thumb {\\n background: #8b8b99; }\\n [vp] [vp-suggestion-screen] ::-webkit-scrollbar-thumb:hover {\\n background: #555; }\\n [vp] [vp-suggestion-screen] .vp-dropdown-suggest {\\n display: nenhum;\\n font-family: \\"Open Sans\\", sans-serif; }\\n [vp] [vp-suggestion-screen] .vp-dropdown-suggest.vp-enabled {\\n exibição: bloco;\\n posição: absoluta;\\n altura: 54px;\\n largura : 120px;\\n estouro: oculto automático;\\n cor: branco;\\n fundo: #003f86; \\n raio da borda: 3px;\\n espessura da fonte: 600;\\n tipo de estilo de lista: nenhum;\\n preenchimento: 0px; }\\n [vp] [vp-suggestion-screen] .vp-dropdown-item {\\n cursor: pointer;\\n font-size: 12px;\\n margin: 5px 0px -4px 6px; }\\n [vp] [tela de sugestão-vp] .vp-dropdown-item:hover {\\n cor: #80b6f3; }\\n [vp] [vp-suggestion-screen] .vp-title {\\n text-align: center;\\n font-size: 14px;\\n font-weight: bold;\\n color : #003f86; }\\n [vp] [vp-suggestion-screen] .vp-text {\\n display: nenhum;\\n flex: 1;\\n align-self: stretch;\\n margin-top: 10px ;\\n margem esquerda: 20px;\\n margem direita: 20px;\\n preenchimento: 8px 10px 6px;\\n tamanho da fonte: 12px;\\n borda: nenhum;\\n raio da borda : 12px;\\n background-color: #efeff2; }\\n [vp] [vp-suggestion-screen] .vp-close-button {\\n margin-top: 8px; }\\n [vp] [tela-sugestão-vp].vp-enabled {\\n esquerda: 0;\\n transição: esquerda 0,4s atenuação 0,8s; }\\n", ""]);\n\n\n//# sourceURL=webpack://VLibras/./plugin/components/SuggestionScreen/suggestion-screen.scss?./node_modules/css-loader/ dist/cjs.js!./node_modules/sass-loader/dist/cjs.js')},"./node_modules/css-loader/dist/cjs.js!./node_modules/sass-loader/dist/cjs. js!./plugin/scss/styles.scss":function(module,exports,__webpack_require__){eval('exports = module.exports = __webpack_require__(/*! ../../node_modules/css-loader/dist/ runtime/api.js */ "./node_modules/css-loader/dist/runtime/api.js")(false);\n// Module\nexports.push([module.i, "[vp] [vp -blue-button] {\\n padding: 7px;\\n border-radius: 6px;\\n font-size: 12px;\\n text-align: center;\\n font-weight: bold;\ \n cor: branco; \\n background-color: #003f86;\\n cursor: pointer;\\n margin: 10px 5px;\\n align-self: center; }\\n\\n[vp] [vp-visualize-signal-button] {\\n padding: 7px;\\n border-radius: 6px;\\n font-size: 12px;\\n text- alinhamento: centro;\\n espessura da fonte: negrito;\\n cor: #003f86;\\n fundo: branco;\\n borda: 1px sólido #003f86;\\n cursor: ponteiro;\\n margem: 10px 5px;\\n align-self: center; }\\n\\n[vp] [vp-sugestion-area] {\\n display: flex; }\\n\\n[vp] [vp-fechar-botão] {\\n exibir: flex;\\n direção flex: linha;\\n justificar conteúdo: centro;\\n alinhar itens: centro;\\n largura: 28px;\\n altura: 28px;\\n raio da borda: 14px;\\n cor de fundo: #b3b3b3;\\n cursor: ponteiro; }\\n [vp] [vp-fechar-botão] img {\\n largura: 100%; }\\n\\n#gameContainer {\\n background: #ebebeb !important; }\\n", ""]);\n\n\n//# sourceURL=webpack: }\\n [vw] [vw-access-button] span {\\n font-size: 13px;\\n color: #004088; }\\n [vw] [vw-access-button] .access-button:hover + .pop-up {\\n width: 68%; }\\n [vw] [vw-access-button] .pop-up {\\n float: right;\\n width: 0%;\\n margin-right: 6px;\\n -webkit-transition : largura 2s;\\n /* Para Safari 3.1 a 6.0 */\\n transição: largura 0,5s; }\\n [vw] [vw-access-button] .pop-up.left {\\n float: left !important;\\n margin-right: 0px; }\\n [vw] [vw-access-button] .access-button.left {\\n float: left !important;\\n margin-right: 6px; }\\n", ""]);\n\n\n//# sourceURL=webpack://VLibras/./widget/src/components/AccessButton/styles.scss?./node_modules/css-loader/ dist/cjs.js!./node_modules/sass-loader/dist/cjs.js')},"./node_modules/css-loader/dist/cjs.js!./node_modules/sass-loader/dist/cjs. js!. 500;\\n box-shadow: 0 2px 1px #bcbcbc;\\n z-index: 4; }\\n .vw-links .vw-tooltip__item:após {\\n conteúdo: \\"\\";\\n exibição: bloco;\\n posição: absoluta;\\n largura: 0;\\ n altura: 0;\\n estilo de borda: sólido; }\\n .vw-links .vw-tooltip__initiator {\\n cursor: ponteiro;\\n z-index: 5; }\\n .vw-links .vw-tooltip .vw-tooltip__item {\\n top: calc(100% + 1em);\\n left: 50%;\\n transform: translate3d(-50%, - 15px, 0); }\\n .vw-links .vw-tooltip .vw-tooltip__item:after {\\n top: -0.5em;\\n left: 50%;\\n transform: translate3d(-50%, 0, 0 );\\n border-width: 0 0.5em 0.5em 0.5em;\\n border-color: transparente transparente branco transparente;\\n -webkit-filter: drop-shadow(1px 2px 1px #bcbcbc);\\ n filtro: drop-shadow(1px -1px 1px #bcbcbc); }\\n .vw-links .vw-tooltip.active > .vw-tooltip__item {\\n transform: translate3d(-50%, 0, 0);\\n visibilidade: visível;\\n opacidade: 1; }\\n\\n[vw].maximize [vw-plugin-wrapper] {\\n largura: 100%;\\n altura: 100% !important;\\n max-height: inicial !important; }\\n\\n[vw].left [vw-plugin-wrapper] {\\n float: left; }\\n\\n[vw] [vw-plugin-wrapper] {\\n display: nenhum;\\n width: 300px;\\n height: 100%;\\n float: right;\\n estouro: oculto;\\n plano de fundo: branco;\\n -moz-border-radius: 20px;\\n -webkit-border-radius: 20px;\\n -webkit-box-shadow: 0px 0px 9px 0px rgba (0, 0, 0, 0.15);\\n -moz-box-shadow: 0px 0px 9px 0px rgba(121, 76, 76, 0.15);\\n box-shadow: 0px 0px 9px 0px rgba(0, 0, 0, 0,15); }\\n [vw] [vw-plugin-wrapper].ativo {\\n display: -webkit-flex;\\n display: flex; \\n flex-direction: coluna;\\n -webkit-flex-direction: coluna;\\n height: 450px;\\n max-width: 100%;\\n overflow: hidden;\\n min- altura: 100%; }\\n", ""]);\n\n\n//# sourceURL=webpack://VLibras/./widget/src/components/PluginWrapper/styles.scss?./node_modules/css-loader/ dist/cjs.js!./node_modules/sass-loader/dist/cjs.js')},"./node_modules/css-loader/dist/cjs.js!./node_modules/sass-loader/dist/cjs. js!./widget/src/scss/reset.scss":function(module,exports,__webpack_require__){eval('exports = module.exports = __webpack_require__(/*! ../../../node_modules/css -loader/dist/runtime/api.js */ "./node_modules/css-loader/dist/runtime/api.js")(false);\n// Module\nexports.push([module.i, " [vw] {\\n /* Redefinição da função de exibição HTML5 para navegadores mais antigos */ }\\n [vw] html, [vw] corpo, [vw] div, [vw] span, \\n [vw] hora, [vw] marca, [vw] áudio, [vw] vídeo {\\n margem: 0;\\n preenchimento: 0;\\n borda: 0;\\n tamanho da fonte : 100%;\\n fonte: herança;\\n alinhamento vertical: linha de base;\\n exibição: bloco; }\\n [vw] artigo, [vw] à parte, [vw] detalhes, [vw] figcaption, [vw] figura,\\n [vw] rodapé, [vw] cabeçalho, [vw] hgroup, [vw] menu, [vw] nav, seção [vw] {\\n exibição: bloco; }\\n [vw] img {\\n largura: não definido; }\\n [vw] corpo {\\n altura da linha: 1; }\\n [vw] ol, [vw] ul {\\n estilo de lista: nenhum; }\\n [vw] blockquote, [vw] q {\\n aspas: nenhum; }\\n [vw] blockquote:antes, [vw] blockquote:depois,\\n [vw] q:antes, [vw] q:depois {\\n conteúdo: \'\';\\n conteúdo: nenhum; }\\n [vw] tabela {\\n border-collapse: recolher;\\n border-spacing: 0; }\\n", ""]);\n\n\n//# sourceURL=webpack://VLibras/./widget/src/scss/reset. }\\n div[vw] [vw-plugin-wrapper].ativo {\\n altura: 358px !important;\\n largura: 210px !important; }\\n #gameContainer {\\n display: flex !important;\\n justify-content: center !important;\\n top: -75px !important;\\n left: -90px !important;\\n largura: 100% !importante;\\n altura: 70% !importante; }\\n [vp] [vp-controls] {\\n esquerda: 36% !important;\\n bottom: 20% !important;\\n background: branco; }\\n [vp] .vpw-controls-slider {\\n largura: 27% !importante; }\\n [vp] .vpw-box .vpw-mes {\\n esquerda: 36% !important;\\n tamanho da fonte: 12px !important; }\\n [vp] .vpw-settings-btn .vpw-settings-btn-close,\\n [vp] .vpw-btn-close .vpw-img {\\n direita: 100px; }\\n [vp] [vp-rate-box].vp-enabled .vp-container {\\n margin-right: 70px;\\n margin-bottom: 80px; }\\n [vp] [vp-rate-button] . botão vp {\\n margem direita: 110px;\\n margem inferior: 130px; }\\n [vp] [vp-rate-button] .vp-button img {\\n width: 32px;\\n margin-bottom: -37px;\\n margin-right: -14px; }\\n [vp] .vpw-message-box {\\n tamanho da fonte: 0,7em; }\\n [vp] .vpw-message-box .vpw-message {\\n margem esquerda: 10px;\\n margem direita: 93px;\\n margem inferior: 3px; }\\n [vp] .vpw-settings .vpw-content .vpw-vlibras-logo {\\n esquerda: 39%;\\n inferior: 125px; }\\n [vp] .vpw-settings .vpw-content > ul {\\n margin-left: -20px; }\\n [vp] .vpw-settings .vpw-content > ul li .vpw-name {\\n tamanho da fonte: 12px; }\\n [vp] .vpw-settings .vpw-content > ul .vpw-localism .vpw-flag {\\n direita: 88px;\\n altura: 24px; }\\n [vp] .vpw-switch {\\n largura: 48px;\\n altura: 15px;\\n margem superior: -10px;\\n direita: 64px; }\\n [vp] . vpw-slider-check.vpw-round:before {\\n top: -3px; }\\n [vp] .vpw-slider-check:before {\\n height: 21px;\\n width: 20px;\\n left: -20;\\n bottom: 4px; }\\n [vp] .vpw-settings .vpw-content .vpw-vlibras-logo .vpw-logo {\\n largura: 51%;\\n margem esquerda: 32px; }\\n [vp] .vpw-settings .vpw-content .vpw-vlibras-logo .vpw-vlibras-span {\\n esquerda: 44%;\\n inferior: 5%; }\\n [vp] .vpw-settings .vpw-content .vpw-vlibras-logo {\\n left: 39%;\\n transform: translateX(-50%);\\n bottom: 125px; }\\n [vp] .vpw-settings .vpw-content > ul .vpw-localism .vpw-abbrev {\\n direita: 65px; }\\n [vp] .vpw-controls-dictionary:not(.vpw-loading-dictionary):antes de {\\n tamanho da fonte: 12px; }\\n [vp] .vpw-info-screen #vpw-info-tab-bullets {\\n direita: 47px;\\n inferior: 100px; }\\n [vp] .vpw-info-screen .vpw-seta-direita. ativo {\\n direita: 94px;\\n posição: absoluto;\\n superior: 137px;\\n z-index: 4; }\\n [vp] .vpw-info-screen .vpw-arrow-left.active {\\n direita: inicial;\\n posição: absoluta;\\n topo: 137px;\\n z-index: 4; }\\n [vp] .vpw-info-screen #vpw-info-main.active {\\n font-size: 0.7125em;\\n margin-right: 99px;\\n margin-bottom: 136px; }\\n [vp] .vpw-info-screen .vpw-info-tab .vpw-info-tab-label {\\n margem superior: -8px;\\n tamanho da fonte: 14px; }\\n [vp] .vpw-info-screen .vpw-info-tab .vpw-info-tab-label-two {\\n tamanho da fonte: 14px;\\n margem superior: 6px; }\\n [vp] .vpw-info-screen #vpw-info-realizadores .vpw-logo.ufpb {\\n largura máxima: 20%; }\\n [vp] .vpw-info-screen #vpw-info-realizadores .vpw-logo-group.text {\\n display: block; }\\n [vp] .vpw-info-screen #vpw-info-realizadores .vpw-logo-group. logo1 {\\n largura máxima: 70%; }\\n [vp] .vpw-info-screen #vpw-info-realizadores .vpw-logo-group.logo2 {\\n max-width: 86%; }\\n [vp] .vpw-info-screen #vpw-info-realizadores.active {\\n font-size: 0.8125em;\\n margin-right: 81px;\\n margin-bottom: 113px; }\\n [vp] .vpw-settings .vpw-content .vpw-flag {\\n altura: 18px; }\\n [vp] .vpw-settings\\n.vpw-content\\n> .vpw-localism\\n.vpw-regions\\n> .vpw-container-regions {\\n margin-top : -1%;\\n margem inferior: -2px; }\\n [vp] .vpw-settings .vpw-content > .vpw-localism {\\n largura: 71%; }\\n [vp] .vpw-settings .vpw-content > .vpw-localism .vpw-regions {\\n left: -3px !important; }\\n [vp] .vpw-settings .vpw-content > ul .vpw-opacity {\\n margem direita: 56px; }\\n [vp] .vpw-dictionary .vpw-dict-container {\\n largura: 64%;\\n altura: 44%;\\n esquerda: 44%; }\\n [vp] .vpw-bar .vpw-title {\\n margem direita: 40px; }\\n [vp] .vpw-dictionary .vpw-panel .vpw-search entrada {\\n largura: 73%;\\n esquerda: 39%; }\\n [vp] .vpw-dictionary .vpw-panel .vpw-search .vpw-icon img {\\n direita: 70px; }\\n [vp] [dicionário vp] {\\n à direita: 18px !important; }\\n [vp] .vpw-dictionary .vpw-dict-container ul li:not(.margin) {\\n padding: 5px; }\\n [vp] [vp-suggestion-button].vp-enabled {\\n direita: 40px;\\n inferior: 150px !important; }\\n [vp] [vp-suggestion-screen] {\\n width: 72%;\\n height: 60%;\\n top: 41px; }\\n [vp] [vp-change-avatar] {\\n esquerda: 9px;\\n superior: 265px; }\\n [vp] [vp-change-avatar] .vp-button-change-avatar {\\n width: 30px;\\n height: 30px; }\\n [vp] [vp-change-avatar] .vp-button-change-avatar .change-avatar-female {\\n width: 15px; }\\n [vp] [vp-change-avatar] .vp-button-change-avatar .change-avatar-male {\\n width: 15px; } }\\n\\ndiv[vw] {\\n posição: fixa;\\n largura máxima: 95vw;\\n direita: 0;\\n superior: 60%;\\n margem superior: -32vh;\\n z-index: 99999999;\\n display: nenhum;\\n font-family: Arial;\\n color: #000;\\n line-height: 1.3; }\\n div[vw].enabled {\\n display: bloco; }\\n div[vw].active {\\n margem superior: -285px; }\\n div[vw].maximize {\\n superior: 6vh;\\n esquerda: 6vh;\\n direita: 6vh;\\n inferior: 6vh;\\n largura máxima: inicial;\\ n margem superior: 0; }\\n div[vw].esquerda {\\n esquerda: 0;\\n direita: inicial; }\\n div[vw] #gameContainer {\\n position: absolute !important;\\n top: -25px;\\n left: 0;\\n right: 0;\\n bottom: 0;\ \n margem: auto !important;\\n altura mínima: calc(0,7 * 450px);\\n largura mínima: calc(0,9 * 300px); \\n largura: 100%;\\n altura: 80%;\\n estouro: oculto;\\n -moz-border-radius: 10px;\\n -webkit-border-radius: 10px;\\n border-radius: 0px;\\n background: nenhum !important; }\\n div[vw] #gameContainer canvas {\\n altura mínima: calc(0,7 * 450px);\\n largura mínima: calc(0,9 * 300px);\\n altura: 100%;\\ n largura: 100%;\\n -webkit-filter: blur(0px);\\n -moz-filter: blur(0px);\\n -ms-filter: blur(0px);\\n -o -filter: blur(0px);\\n filtro: blur(0px); }\\n div[vw] [vp] {\\n posição: relativa;\\n altura: 100%;\\n largura: 100%;\\n largura mínima: 300px;\\n altura mínima : 450px;\\n z-index: 1;\\n estouro: oculto; }\\n div[vw] [vp] [vp-message-box] {\\n position: absolute;\\n z-index: 5; }\\n div[vw] [vp] [vp-info-screen] {\\n z-index: 3; }\\n div[vw] [vp] [vp-info-screen] #info-text {\\n font-size: 1em !important; }\\n div[vw] [vp] [vp-info-screen] #info-realizadores {\\n font-size: 11px !important; }\\n div[vw] [vp] [vp-controls] {\\n position: absolute;\\n display: flex;\\n align-items: center;\\n justify-content: center;\ \n left: 50%;\\n transform: translateX(-50%);\\n bottom: 0%;\\n max-width: 900px;\\n z-index: 2; }\\n div[vw] [vp] [vp-settings-btn] {\\n z-index: 2; }\\n div[vw] [vp] [dicionário vp] {\\n índice z: 8; }\\n div[vw] [vp] [vp-rate-box] {\\n bottom: 58px; }\\n div[vw] [vp] [vp-suggestion-button].vp-enabled,\\n div[vw] [vp] [vp-suggestion-button] {\\n bottom: 58px; }\\n div[vw] [vp] [vp-suggestion-screen] {\\n top: 41px; }\\n\\n.vw-text: hover {\\n cor de fundo: rgba(0, 63, 134, 0.2);\\n cor: #000;\\n cursor: ponteiro; }\\n\\n.vw-text-active {\\n cor de fundo: rgba(30, 63, 134, 0,2);\\n cor: #000; }\\n", ""]);\n\n\n//# sourceURL=webpack://VLibras/./widget/src/scss/styles.scss?./node_modules/css-loader/dist/ cjs.js!./node_modules/sass-loader/dist/cjs.js')},"./node_modules/css-loader/dist/runtime/api.js":function(module,exports,__webpack_require__){"use strict";eval('\n\n/*\n Licença MIT http://www.opensource.org/licenses/mit-license.php\n Autor Tobias Koppers @sokra\n*/\n// css base código, injetado pelo css-loader\n// eslint-disable-next-line func-names\nmodule.exports = function (useSourceMap) {\n var list = []; // retorna a lista de módulos como string css \n\n list.toString = function toString() {\n retornar isso. map(function (item) {\n var content = cssWithMappingToString(item, useSourceMap);\n\n if (item[2]) {\n return "@media ".concat(item[2], "{") .concat(conteúdo, "}");\n }\n\n retorna conteúdo;\n }).join(\'\');\n }; // importa uma lista de módulos para a lista\n // eslint-disable-next-line func-names\n\n\n list.i = function (modules, mediaQuery) {\n if (typeof modules === \'string\') {\n // eslint-disable-next-line no-param-reatribuir\n modules = [[null, modules, \'\']];\n }\n\n var jáImportedModules = {};\n\n for (var i = 0; i < this.length; i++) {\n // eslint-disable-next-line prefer-destructuring\n var id = this[i][0]; \n\n if (id != null) {\n jáImportedModules[id] = true;\n }\n }\n\n for (var _i = 0; _i < módulos.comprimento; _i++) {\n var item = módulos[_i]; // pular o módulo já importado\n // esta implementação não é 100% perfeita para combinações estranhas de consulta de mídia\n // quando um módulo é importado várias vezes com diferentes consultas de mídia.\n // Espero que isso nunca ocorra (Ei assim temos pacotes menores)\n\n if (item[0] == null || !alreadyImportedModules[item[0]]) {\n if (mediaQuery && !item[2]) {\n item[2 ] = mediaQuery;\n } else if (mediaQuery) {\n item[2] = "(".concat(item[2], ") e (").concat(mediaQuery, ")");\n } \n\n list.push(item);\n }\n }\n };\n\n return list;\n};\n\nfunction cssWithMappingToString(item, useSourceMap) {\n var content = item[ 1] || \'\'; \n//\n// O aviso de direitos autorais acima e este aviso de permissão devem ser incluídos\n// em todas as cópias ou partes substanciais do Software.\n//\n// O SOFTWARE É FORNECIDO \"COMO ESTÁ\ ", SEM GARANTIA DE QUALQUER TIPO, EXPRESSA\n// OU IMPLÍCITA, INCLUINDO MAS NÃO SE LIMITANDO ÀS GARANTIAS DE\n// COMERCIABILIDADE, ADEQUAÇÃO A UM DETERMINADO FIM E NÃO VIOLAÇÃO. EM\n// EM NENHUM CASO OS AUTORES OU DETENTORES DOS DIREITOS AUTORAIS SERÃO RESPONSÁVEIS POR QUALQUER RECLAMAÇÃO,\n// DANOS OU OUTRA RESPONSABILIDADE, SEJA EM UMA AÇÃO DE CONTRATO, ILÍCITO OU\n// DE OUTRA FORMA, DECORRENTE DE, DE OU EM CONEXÃO COM O SOFTWARE OU O\n// USO OU OUTROS NEGÓCIOS NO SOFTWARE.\n\n\n\nvar R = typeof Reflect === 'object' ? Reflect : null\nvar ReflectApply = R && typeof R.apply === 'função'\n ? R.apply\n : função ReflectApply(alvo, receptor, args) {\n return Function.prototype.apply.call(target, receiver, args);\n }\n\nvar ReflectOwnKeys\nif (R && typeof R.ownKeys === 'function') {\n ReflectOwnKeys = R.ownKeys\n} else if (Object.getOwnPropertySymbols) {\n ReflectOwnKeys = function ReflectOwnKeys(destino) {\n return Object.getOwnPropertyNames(destino)\n .concat(Object.getOwnPropertySymbols(destino));\n }; \n} else {\n ReflectOwnKeys = function ReflectOwnKeys(target) {\n return Object.getOwnPropertyNames(target);\n };\n}\n\nfunction ProcessEmitWarning(warning) {\n if (console && console.warn ) console.warn(aviso);\n}\n\nvar NumberIsNaN = Number.isNaN || function NumberIsNaN(value) {\n return value !== value;\n}\n\nfunction EventEmitter() {\n EventEmitter.init.call(this);\n}\nmodule.exports = EventEmitter;\n\ n// Compatibilidade com o nó 0.10. x\nEventEmitter.EventEmitter = EventEmitter;\n\nEventEmitter.prototype._events = indefinido;\nEventEmitter.prototype._eventsCount = 0;\nEventEmitter.prototype._maxListeners = indefinido;\n\n// Por padrão, EventEmitters imprimirá um aviso se mais de 10 ouvintes forem\n// adicionados a ele. Este é um padrão útil que ajuda a encontrar vazamentos de memória.\nvar defaultMaxListeners = 10;\n\nObject.defineProperty(EventEmitter, 'defaultMaxListeners', {\n enumerable: true,\n get: function() {\n return defaultMaxListeners; \n },\n set: function(arg) {\n if (typeof arg !== 'number' || arg < 0 || NumberIsNaN(arg)) {\n throw new RangeError('The value of \" defaultMaxListeners\" está fora do intervalo. Deve ser um número não negativo. Received ' + arg + '.');\n }\n defaultMaxListeners = arg;\n }\n}); \n\nEventEmitter.init = function() {\n\n if (this._events === indefinido ||\n this._events === Object.getPrototypeOf(this)._events) {\n this._events = Object.create(null);\n this._eventsCount = 0;\n }\n\n this._maxListeners = this._maxListeners || undefined;\n};\n\n// Obviamente nem todos os Emissores devem ser limitados a 10. Esta função permite\n// que isso seja aumentado. Defina como zero para ilimitado.\nEventEmitter.prototype.setMaxListeners = function setMaxListeners(n) {\n if (typeof n !== 'number' || n < 0 || NumberIsNaN(n)) {\n throw new RangeError( 'O valor de \"n\" está fora do intervalo. Deve ser um número não negativo. Recebido ' + n + '.');\n }\n this._maxListeners = n;\n return this;\ n};\n\nfunction $getMaxListeners(that) {\n if (that._maxListeners === indefinido)\n return EventEmitter. \n arremessador; // Evento 'error' não tratado\n }\n // Pelo menos forneça algum tipo de contexto ao usuário\n var err = new Error('Erro não tratado.' + (er ? ' (' + er.message + ' )' : ''));\n err.context = er;\n throw err; // Evento 'erro' não tratado\n }\n\n var handler = events[type];\n\n if (handler === undefined)\n return false;\n\n if (typeof handler === 'function') {\n ReflectApply(handler, this, args);\n } else {\n var len = handler.length;\n var listeners = arrayClone(handler, len);\n for (var i = 0 ; i < len; ++i)\n ReflectApply(listeners[i], this, args);\n }\n\n return true;\n};\n\nfunction _addListener(target, type, listener, prepend ) {\n var m;\n var eventos;\n var existente;\n\n if (typeof listener !== 'function' ) {\n throw new TypeError('O argumento \"listener\" deve ser do tipo Function. Tipo recebido ' + typeof listener);\n }\n\n events = target._events;\n if (events == = indefinido) {\n events = target._events = Object.create(null);\n target._eventsCount = 0;\n } else {\n // Para evitar recursão caso digite === \"newListener \"! Antes\n // de adicioná-lo aos ouvintes, primeiro emita \"newListener\".\n if (events.newListener !== undefined) {\n target.emit('newListener', type,\n listener.listener ? listener.listener : listener);\n\n // Reatribua `events` porque um manipulador newListener pode ter feito com que\n // this._events fosse atribuído a um novo objeto\n events = target._events;\ n }\n existente = eventos[tipo]; \n }\n\n if (existente === indefinido) {\n // Otimiza o caso de um ouvinte. Não precisa do objeto de matriz extra.\n existente = eventos[tipo] = ouvinte;\n ++target._eventsCount;\n } else {\n if (tipo de existente === 'função') {\n / / Adicionando o segundo elemento, precisa mudar para array.\n existente = eventos[tipo] =\n preceder ? [listener, existing] : [existing, listener];\n // Se já tivermos um array, basta anexar.\n } else if (prepend) {\n existindo.unshift(listener);\n } else {\n existing.push(listener);\n }\n\n // Verifica se há vazamento de ouvinte\nm = $getMaxListeners(target);\n if (m > 0 && existindo.comprimento > m && !existing.warned ) {\n existente.aviso = verdadeiro; argumento deve ser do tipo Function. Tipo recebido ' + typeof listener);\n }\n this.on(type, _onceWrap(this, type, listener));\n return this;\n};\n\nEventEmitter.prototype.prependOnceListener =\n function prependOnceListener(type, listener) {\n if (typeof listener !== 'function') {\n throw new TypeError('O argumento \"listener\" deve ser do tipo Function. Tipo recebido ' + typeof listener);\ n }\n this.prependListener(type, _onceWrap(this, type, listener));\n return this;\n };\n\n// Emite um evento 'removeListener' se e somente se o listener foi removido. \nEventEmitter.prototype.removeListener =\n function removeListener(type, listener) {\n var list, events, position, i, originalListener;\n\n if (typeof listener !== 'function'er(type, listeners[i]);\n }\n }\n\n return this;\n };\n\nfunction _listeners(target, type, unwrap) {\n var events = target._events;\ n\n if (eventos === indefinido)\n return [];\n\n var evlistener = eventos[tipo];\n if (evlistener === indefinido)\n return [];\n\n if (typeof evlistener === 'function')\n return unwrap ? [evlistener.listener || evlistener] : [evlistener];\n\n return unwrap ?\n unwrapListeners(evlistener) : arrayClone(evlistener, evlistener.length);\n}\n\nEventEmitter.prototype.listeners = function listeners(type) {\n return _listeners(this, type, true);\n};\n\nEventEmitter.prototype.rawListeners = function rawListeners(type) {\n return _listeners(this, type, false);\n};\n\nEventEmitter. listenerCount = function(emitter, type) {\n if (typeof emitter.listenerCount === 'function' ) {\n return emitter.listenerCount(tipo);\n } else {\n return listenerCount.call(emissor, tipo);\n }\n};\n\nEventEmitter.prototype.listenerCount = listenerCount;\nfunction listenerCount (tipo) {\n var eventos = this._events;\n\n if (eventos !== indefinido) {\n var evlistener = eventos[tipo];\n\n if (tipo de evlistener === 'função' ) {\n return 1;\n } else if (evlistener !== indefinido) {\n return evlistener.length;\n }\n }\n\n return 0;\n}\n\nEventEmitter.prototype. eventNames = function eventNames() {\n return this._eventsCount > 0 ? ReflectOwnKeys(this._events) : [];\n};\n\nfunction arrayClone(arr, n) {\n var copy = new Array(n);\n for (var i = 0; i < n; + +i)\n copy[i] = arr[i];\n return copy;\n}\n\nfunction spliceOne(list, index) {\n for (; index + 1 < list.length; index++)\n list[index] = list[index + 1];\n list.pop();\n}\n\nfunction unwrapListeners(arr) {\n var ret = new Array(arr.length);\ n for (var i = 0; i < ret.length; ++i) {\n ret[i] = arr[i].listener || arr[i];\n }\n return ret;\n}\n\n\n//# sourceURL=webpack://VLibras/./node_modules/events/events.js?")},"./ node_modules/hasharray/index.js":function(module,exports,__webpack_require__){eval('module.exports = __webpack_require__(/*! ./src/HashArray.js */ "./node_modules/hasharray/src/HashArray. js");\n\n//# sourceURL=webpack://VLibras/./node_modules/hasharray/index.js?')},"./node_modules/hasharray/src/HashArray.js":function(module ,exports,__webpack_require__){eval(" \n \n this.options = opções || {\n ignoreDuplicates: false\n };\n\n if (callback) {\n callback('construct');\n }\n },\n //----------- -------------------------\n // add()\n //----------------------- --------------------\n addOne: function (obj) {\n var needsDupCheck = false;\n for (var key in this.keyFields) {\n chave = this.keyFields[chave];\n var inst = this.objectAt(obj, chave);\n if (inst) {\n if (this.has(inst)) {\n if (this.options. ignoreDuplicates)\n return;\n if (this._map[inst].indexOf(obj) != -1) {\n // Não é possível adicionar o mesmo item duas vezes\n needsDupCheck = true;\n continue;\n } \n this._map[inst].push(obj);\n }\n else this._map[inst] = [obj];\n }\n }\n\n if (!needsDupCheck || this._list . \n //----------------------------------\n /**\n * Retorna um novo HashArray que contém a interseção entre este (A) e o hasharray passado em (B). Retorna A ^ B.\n */\n interseção: função (outro) {\n var self = this;\n\n if (!other || !other.isHashArray)\n throw Error('Cannot HashArray.intersection () em um objeto não hasharray. Você passou: ', other);\n\n var ret = this.clone(null, true),\n allItems = this.clone(null, true).addAll(this .all.concat(other.all));\n\n allItems.all.forEach(função (item) {\n if (self.collides(item) && other.collides(item))\n ret.add( item);\n });\n\n return ret;\n },\n /**\n * Retorna um novo HashArray que contém o complemento (diferença) entre este hash array (A) e o hasharray passado (B). Retorna A - B.\n */\n complemento: function (other) {\n var self = this;\n\n if (!other || !other.isHashArray)\n throw Error('Não é possível HashArray.complement() em um objeto não-hasharray. Você passou: ', outro);\n\n var ret = this.clone(null, true);\n\n this.all.forEach(function (item) {\n if (!other.collides(item))\n ret.add(item);\n });\n\n return ret;\n },\n //----------------------- ------------\n // Recuperação\n //----------------------- ------\n get: function(key) {\n return (!(this._map[key] instanceof Array) || this._map[key].length != 1) ? this._map[key] : this._map[key][0];\n },\n getAll: function(keys) {\n keys = keys instanceof Array ? keys : [keys];\n\n if (keys[0] == '*')\n return this.all;\n\n var res = new HashArray(this.keyFields);\n for (var key nas chaves)\n res.add.apply(res, this. getAsArray(keys[key]));\n\n return res.all;\n },\n getAsArray: function(key) {\n return this._map[key] || [];\n },\n getUniqueRandomIntegers: function (count, min, max) {\n var res = [], map = {};\n\n count = Math.min(Math.max(max - min , 1), contagem);\n \n while (res.length < contagem)\n {\n var r = Math.floor(min + (Math.random() * (max + 1)));\n if (map[r]) continue;\n map[r] = true;\n res.push(r);\n }\n\n return res;\n },\n sample: function (count, keys ) {\n // http://en.wikipedia.org/wiki/Image_(mathematics)\n var image = this.all,\n ixs = {},\n res = [];\n\n if (chaves)\n image = this.getAll(keys);\n\n var rand = this.getUniqueRandomIntegers(count, 0, image.length - 1);\n\n for (var i = 0; i < rand .length; i++)\n res.push(image[rand[i]]);\n\n return res; callback) {\n this.callback('remove', old);\n }\n \n return this;\n },\n //---------------- -------------------\n // Utilitário\n //---------------------- -------------\n objectAt: function(obj, path) {\n if (typeof path === 'string') {\n return obj[path];\n }\ n\n var dup = path.concat();\n // else assume array.\n while (dup.length && obj) {\n obj = obj[dup.shift()];\n }\n\ n return obj;\n },\n //----------------------------------\n / / Iteração\n //----------------------------------\n forEach: function(keys, callback ) {\n keys = instância de keys de Array ? keys : [keys];\n\n var objs = this.getAll(keys);\n\n objs.forEach(callback);\n \n return this;\n },\n forEachDeep: function(keys, chave, retorno de chamada) {\n chaves = instância de chaves de Array ? teclas : [teclas]; \n return ret;\n },\n average: function(keys, key, weightKey) {\n var ret = 0,\n tot = 0,\n weightsTotal = 0,\n self = this;\n\ n if (weightKey !== indefinido)\n this.forEachDeep(chaves, weightKey, função (valor) {\n weightsTotal += valor;\n })\n\n this.forEachDeep(chaves, chave, função (valor , item) {\n if (weightKey !== indefinido)\n valor *= (self.objectAt(item, weightKey) / weightsTotal);\n\n ret += valor;\n tot++;\n }); \n\n return weightKey !== indefinido ? ret : ret / tot;\n },\n //-----------------------------------\ n // Filtragem\n //----------------------------------\n filtro: função (teclas , callbackOrKey) {\n var self = this;\n \n var callback = (typeof(callbackOrKey) == 'função') ? callbackOrKey : defaultCallback; \n\n // adiciona a entrada _subClasses\n BaseClass._subClasses = [];\n\n // esvazia o método init\n BaseClass.prototype.init = function(){};\n\n\n /* *\n * Mecanismo estendido. Retorna uma classe derivada.\n *\n * @param {object} instanceMembers - Membros que serão propriedade de instâncias.\n * @param {object} classMembers - Membros que serão propriedade da própria classe.\n * @ retorna {JClass}\n */\n BaseClass._extend = function(instanceMembers, classMembers, opções) {\n\n // argumentos padrão\n if (instanceMembers === undefined) {\n instanceMembers = {};\ n }\n if (classMembers === indefinido) {\n classMembers = {};\n }\n if (options === indefinido) {\n options = {};\n }\n\n // mixin opções padrão\n opções = extend({}, defaultOptions, opções); criar e retornar nossa nova classe\n return BaseClass._extend(instanceMembers, {}, opções);\n };\n\n\n /**\n * Retorna uma instância de uma classe com uma lista de argumentos. Isso fornece um uso de construtor\n * do tipo apply. Observe que esta abordagem não funciona com construtores nativos (por exemplo, String\n * ou Boolean).\n *\n * @param {Class|JClass} cls - A classe a ser instanciada. Pode ser uma JClass ou uma classe\n * baseada em protótipo.\n * @param {array} [args=[]] - Argumentos a serem passados ​​para o construtor.\n * @returns {instance}\n */\ n BaseClass._construct = function(cls, args) {\n // argumentos padrão vazios\n if (args === indefinido) {\n args = [];\n }\n\n // cria um wrapper de classe que chama cls como uma função\n var Class = function() {\n return cls.apply(this, args);\n };\n\n // copia o protótipo\n Class. \r\n\r\n\t\t// getBoundingClientRect contém rolagem à esquerda no Chrome no Android.\r\n\t\t// Não encontrei uma detecção de recurso que comprove isso. Pior caso\r\n\t\t// cenário de incompatibilidade: o recurso 'tap' em controles deslizantes horizontais quebra.\r\n\t\tif ( /webkit.*Chrome.*Mobile/i.test( navigator.userAgent) ) {\r\n\t\t\tpageOffset.x = 0;\r\n\t\t}\r\n\r\n\t\treturn {\r\n\t\ t\ttop: rect.top + pageOffset.y - docElem.clientTop,\r\n\t\t\tleft: rect.left + pageOffset.x - docElem.clientLeft\r\n\t\t};\r \n\t}\r\n\r\n\t// Verifica se um valor é numérico.\r\n\tfunction isNumeric ( a ) {\r\n\t\treturn typeof a === 'number ' && !isNaN( a ) && isFinite( a );\r\n\t}\r\n\r\n\t// Define uma classe e a remove após [duração] ms.\r\n\tfunction addClassFor ( elemento, className, duração ) {\r\n\t\taddClass(elemento, className); \r\n\t\tsetTimeout(function(){\r\n\t\t\tremoveClass(elemento, className);\r\n\t\t}, duração);\r\n\t}\ r\n\r\n\t// Limita um valor a 0 - 100\r\n\tlimite de função ( a ) {\r\n\t\treturn Math.max(Math.min(a, 100), 0);\r\n\t}\r\n\r\n\t// Envolve uma variável como um array, se ainda não o for.\r\n\tfunction asArray ( a ) {\r \n\t\treturn Array.isArray(a) ? a : [a];\r\n\t}\r\n\r\n\t// Conta decimais\r\n\tfunction countDecimals ( numStr ) {\r\n\t\tvar pedaços = numStr. split(\".\");\r\n\t\tretornar peças.comprimento > 1 ? pedaços[1].comprimento: 0;\r\n\t}\r\n\r\n\t// http://youmightnotneedjquery.com/#add_class\r\n\tfunção addClass ( el, className ) {\r\n\t\tif ( el.classList ) {\r\n\t\t\tel.classList.add(className);\r\n\t\t} else {\r\n\t \t\tel.className += ' ' + className;\r\n\t\t}\r\n\t}\r\n\r\n\t// http://youmightnotneedjquery. com/#remove_class\r\n\tfunction removeClass ( el, className ) {\r\n\t\tif ( el.classList ) {\r\n\t\t\tel.classList.remove(className);\ r\n\t\t} else {\r\n\t\t\tel.className = el.className.replace(new RegExp('(^|\\\\b)' + className.split(' ' ).join('|') + '(\\\\b|$)', 'gi'), ' ');\r\n\t\t}\r\n\t}\r\n \r\n\t// https://plainjs.com/javascript/attributes/adding-removing-and-testing-for-classes-9/\r\n\tfunction hasClass ( el, className ) {\r\ n\t\treturn el.classList ? el.classList.contains(className) : new RegExp('\\\\b' + className + '\\\\b').test(el.className);\r\n\t}\r\n\ r\n\t// https://developer.mozilla.org/en-US/docs/Web/API/Window/scrollY#Notes\r\n\tfunction getPageOffset ( ) {\r\n\r\n \t\tvar supportPageOffset = window.pageXOffset !== indefinido,\r\n\t\t\tisCSS1Compat = ((document. modo compatível || \"\") === \"CSS1Compat\"),\r\n\t\t\tx = supportPageOffset ? window.pageXOffset: isCSS1Compat? document.documentElement.scrollLeft : document.body.scrollLeft,\r\n\t\t\ty = supportPageOffset ? window.pageYOffset: isCSS1Compat? document.documentElement.scrollTop : document.body.scrollTop;\r\n\r\n\t\treturn {\r\n\t\t\tx: x,\r\n\t\t\ty: y \r\n\t\t};\r\n\t}\r\n\r\n\t// fornecemos uma função para calcular constantes em vez de\r\n\t// acessar a janela.* assim que o módulo precisar\r\n\t// para que não possamos calcular nada se não for necessário\r\n\tfunction getActions ( ) {\r\n\r\n\t\t// Determinar os eventos a vincular. O IE11 implementa pointerEvents sem\r\n\t\t// um prefixo, o que quebra a compatibilidade com a implementação do IE10.\r\n\t\treturn window.navigator.pointerEnabled ? {\r\n\t\t\tstart: 'ponteiro para baixo', \r\n\t\t\tmove: 'pointermove',\r\n\t\t\tend: 'pointerup'\r\n\t\t} : window.navigator.msPointerEnabled ? {\r\n\t\t\tstart: 'MSPointerDown',\r\n\t\t\tmove: 'MSPointerMove',\r\n\t\t\tend: 'MSPointerUp'\r\n\ t\t} : {\r\n\t\t\tstart: 'mousedown touchstart',\r\n\t\t\tmove: 'mousemove touchmove',\r\n\t\t\tend: ' mouseup touchend'\r\n\t\t};\r\n\t}\r\n\r\n\r\n// Cálculo do valor\r\n\r\n\t// Determinar o tamanho de um subintervalo em relação a um intervalo completo.\r\n\tfunction subRangeRatio ( pa, pb ) {\r\n\t\treturn (100 / (pb - pa));\r\n\t }\r\n\r\n\t// (porcentagem) Quantos por cento é este valor deste intervalo?\r\n\tfunction fromPercentage ( range, value ) {\r\n\t\treturn (value * 100) / (intervalo[1] - intervalo[0]); \r\n\r\n\t\t// NaN também será avaliado como falso, mas para manter o registro limpo, defina a etapa explicitamente. Certifique-se de\r\n\t\t// não substituir a configuração 'step' por false.\r\n\t\tif ( !percentage ) {\r\n\t\t\tif ( !isNaN( valor[1] ) ) {\r\n\t\t\t\tthat.xPassos[0] = valor[1];\r\n\t\t\t}\r\n\t\t} else {\r\n\t\t\tthat.xSteps.push( isNaN(valor[1]) ? false : valor[1] );\r\n\t\t}\r\n\t}\ r\n\r\n\tfunction handleStepPoint ( i, n, that ) {\r\n\r\n\t\t// Ignorar 'false' step.\r\n\t\tif ( !n ) {\r\n\t\t\treturn true;\r\n\t\t}\r\n\r\n\t\t// Razão de fator para intervalo\r\n\t\tthat.xSteps [i] = dePorcentagem([\r\n\t\t\t que.xVal[i]\r\n\t\t\t,que.xVal[i+1]\r\n\t\t ], n) / subRangeRatio (\r\n\t\t\tthat.xPct[i],\r\n\t\t\tthat.xPct[i+1] ); \r\n\t}\r\n\r\n\r\n// Interface\r\n\r\n\t// A interface para o Spectrum lida com todas as direções\r\n\t/ / conversões, portanto, os valores acima não são conhecidos.\r\n\r\n\tfunction Spectrum ( entrada, snap, direção, singleStep ) {\r\n\r\n\t\tthis.xPct = [];\ r\n\t\tthis.xVal = [];\r\n\t\tthis.xSteps = [ singleStep || false ];\r\n\t\tthis.xNumSteps = [ false ];\r\n\r\n\t\tthis.snap = snap;\r\n\t\tthis.direction = direção;\r \n\r\n\t\tvar índice, ordenado = [ /* [0, 'min'], [1, '50%'], [2, 'max'] */ ];\r\n\ r\n\t\t// Mapeia as chaves do objeto para um array.\r\n\t\tfor ( index in entry ) {\r\n\t\t\tif ( entry.hasOwnProperty(index) ) { \r\n\t\t\t\tordered.push([entrada[índice], índice]);\r\n\t\t\t}\r\n\t\t}\r\n\ r\n\t\t// Classifica todas as entradas por valor (classificação numérica).\r\n\t\tif (ordered.length && typeofordered[0][0] === \"object\" comprimento === 2 ? fromPercentage(this.xVal, value): false;\r\n\t};\r\n\r\n\tSpectrum.prototype.toStepping = function ( value ) {\r\n\r\n\t\ tvalue = toStepping( this.xVal, this.xPct, value );\r\n\r\n\t\t// Inverte o valor se este for um controle deslizante da direita para a esquerda.\r\n\t\ tif ( this.direction ) {\r\n\t\t\tvalue = 100 - value;\r\n\t\t}\r\n\r\n\t\treturn value;\r\n\ t};\r\n\r\n\tSpectrum.prototype.fromStepping = function ( value ) {\r\n\r\n\t\t// Inverte o valor se este for um controle deslizante da direita para a esquerda .\r\n\t\tif ( this.direction ) {\r\n\t\t\tvalue = 100 - value;\r\n\t\t}\r\n\r\n\t\ treturn fromStepping( this.xVal, this.xPct, value );\r\n\t};\r\n\r\n\tSpectrum.prototype.getStep = function ( value ) {\r\n\r\n \t\t// Encontre a etapa adequada para controles deslizantes rtl pesquisando na direção inversa.\r\n\t\t// Corrige o problema nº 262.\r\n\t\tif ( this.direction ) {\r \n\t\t\tvalor = 100 - valor; \r\n\t\t}\r\n\r\n\t\tvalue = getStep(this.xPct, this.xSteps, this.snap, value );\r\n\r\n\t\ tif ( this.direction ) {\r\n\t\t\tvalue = 100 - value;\r\n\t\t}\r\n\r\n\t\treturn value;\r\n\ t};\r\n\r\n\tSpectrum.prototype.getApplicableStep = function ( value ) {\r\n\r\n\t\t// Se o valor for 100%, retorna o passo negativo duas vezes. \r\n\t\tvar j = getJ(value, this.xPct), offset = value === 100 ? 2 : 1;\r\n\t\treturn [this.xNumSteps[j-2], this.xVal[j-offset], this.xNumSteps[j-offset]];\r\n\t};\ r\n\r\n\t// Teste externo\r\n\tSpectrum.prototype.convert = function ( value ) {\r\n\t\treturn this.getStep(this.toStepping(value));\ r\n\t};\r\n\r\n/*\tCada opção de entrada é testada e analisada. Isso impedirá a validação\r\n\tendless em métodos internos. Esses testes são\r\n\testruturados com um item para cada opção disponível. Uma\r\n\opção pode ser marcada como necessária configurando o sinalizador 'r'.\r\n\tA função de teste é fornecida com três argumentos:\r\n\t\t- O valor fornecido para a opção; \r\n\t\t- Uma referência ao objeto de opções;\r\n\t\t- O nome da opção;\r\n\r\n\tA função de teste retorna false quando um erro é detectado ,\r\n\tor true quando tudo está OK. Ele também pode modificar a opção\r\n\tobject, para garantir que todos os valores possam ser repetidos corretamente em outro lugar. */\r\n\r\n\tvar defaultFormatter = { 'to': function( value ){\r\n\t\treturn value !== undefined && value.toFixed(2);\r\n\ t}, 'from': Number };\r\n\r\n\tfunction testStep ( parsed, entrada ) {\r\n\r\n\t\tif ( !isNumeric( entrada ) ) {\r\ n\t\t\tthrow new Error(\"noUiSlider: 'step' is not numeric.\"); \r\n\t\t}\r\n\r\n\t\t// A opção de passo ainda pode ser usada para definir o passo\r\n\t\t// para controles deslizantes lineares. Substituído se definido em 'intervalo'.\r\n\t\tparsed.singleStep = entrada;\r\n\t}\r\n\r\n\tfunction testRange ( parsed, entrada ) {\r\n\ r\n\t\t// Filtro entrada incorreta.\r\n\t\tif ( typeof entry !== 'objeto' || Array.isArray(entry) ) {\r\n\t\t\tthrow new Error(\"noUiSlider: 'range' is not an object.\");\r\n\t\t}\r\n\r\n\t\t// Captura início ou fim ausente.\r \n\t\tif (entrada.min === indefinido || entrada.max === indefinido) {\r\n\t\t\tthrow new Error(\"noUiSlider: 'min' ou 'max' ausente in 'range'.\");\r\n\t\t}\r\n\r\n\t\t// Captura igual início ou fim.\r\n\t\tif (entrada.min === entry.max ) {\r\n\t\t\tthrow new Error(\"noUiSlider: 'range' 'min' e 'max' não podem ser iguais. == 'string' ) {\r\n\t\t\tthrow new Error(\"noUiSlider: 'behaviour' deve ser uma string contendo opções.\");\r\n\t\t}\r\ n\r\n\t\t// Verifique se a string contém alguma palavra-chave.\r\n\t\t// Nenhuma é necessária.\r\n\t\tvar tap = entry.indexOf('tap' ) >= 0,\r\n\t\t\tdrag = entry.indexOf('drag') >= 0,\r\n\t\t\tfixed = entry.indexOf('fixed') >= 0 ,\r\n\t\t\tsnap = entrada.indexOf('snap') >= 0,\r\n\t\t\thover = entrada.indexOf('hover') >= 0;\r\ n\r\n\t\t// Fix #472\r\n\t\tif ( drag && !parsed.connect ) {\r\n\t\t\tthrow new Error(\"noUiSlider: 'drag ' deve ser usado com 'connect': true.\");\r\n\t\t}\r\n\r\n\t\tparsed.events = {\r\n\t\t\ toque: toque || snap,\r\n\t\t\tdrag: arrastar,\r\n\t\t\tfixed: fixo,\r\n\t\t\tsnap: snap,\r\n\t\t\ thover: \r\n\t}\r\n\r\n\tfunction testCssClasses ( analisado, entrada ) {\r\n\r\n\t\tif ( entrada !== indefinido && tipo de entrada !== 'objeto ' ) {\r\n\t\t\tthrow new Error(\"noUiSlider: 'cssClasses' deve ser um objeto.\");\r\n\t\t}\r\n\r\n\ t\tif ( typeof parsed.cssPrefix === 'string' ) {\r\n\t\t\tparsed.cssClasses = {};\r\n\r\n\t\t\tfor ( chave var em entrada ) {\r\n\t\t\t\tif ( !entry.hasOwnProperty(chave) ) ​​{ continue; }\r\n\r\n\t\t\t\tparsed.cssClasses[chave] = parsed.cssPrefix + entrada[chave];\r\n\t\t\t}\r\n\t\ t} else {\r\n\t\t\tparsed.cssClasses = entrada;\r\n\t\t}\r\n\t}\r\n\r\n\t// Testar todos os desenvolvedores configurações e analise para valores seguros.\r\n\tfunction testOptions (options) {\r\n\r\n\t\t// Para provar uma correção para #537, congele as opções aqui.\r\n \t\t// Se o objeto for modificado, um erro será gerado.\r\n\t\t// Objeto. \r\n\t\t\t\tmarkerLarge: 'marcador grande',\r\n\t\t\t\tmarkerSub: 'marcador-sub',\r\n\t\t\t\tvalor : 'value',\r\n\t\t\t\tvalueHorizontal: 'value-horizontal',\r\n\t\t\t\tvalueVertical: 'value-vertical',\r\n\t\ t\t\tvalueNormal: 'valor-normal',\r\n\t\t\t\tvalueLarge: 'valor-grande',\r\n\t\t\t\tvalueSub: 'valor-sub'\ r\n\t\t\t}\r\n\t\t};\r\n\r\n\t\t// Executa todas as opções por meio de um mecanismo de teste para garantir a correção\r\n\t \t// entrada. Deve-se notar que as opções podem ser modificadas para\r\n\t\t// serem tratadas corretamente. Por exemplo, envolvendo inteiros em arrays.\r\n\t\tObject.keys(tests).forEach(function( name ){\r\n\r\n\t\t\t// Se a opção não estiver definida , mas é necessário, gera um erro.\r\n\t\t\tif (options[name] === undefined && defaults[name] === undefined ) {\r\n\r\n\t \t\t\tif ( testes[nome]. r ) {\r\n\t\t\t\t\tthrow new Error(\"noUiSlider: '\" + nome + \"' é necessário.\");\r\n\t\t\t \t}\r\n\r\n\t\t\t\treturn true;\r\n\t\t\t}\r\n\r\n\t\t\ttests[nome]. t( analisado, opções[nome] === indefinido ? defaults[nome] : opções[nome]);\r\n\t\t});\r\n\r\n\t\t// Avançar pips options\r\n\t\tparsed.pips = options.pips;\r\n\r\n\t\t// Pré-define os estilos.\r\n\t\tparsed.style = analisado. orte? 'top' : 'left';\r\n\r\n\t\treturn parsed;\r\n\t}\r\n\r\n\r\nencerramento de função ( target, options, originalOptions ){ \r\n\tvar\r\n\t\tactions = getActions( ),\r\n\t\t// Todas as variáveis ​​locais para 'closure' são prefixadas com 'scope_'\r\n\t\tscope_Target = destino,\r\n\t\tscope_Locations = [-1, -1],\r\n\t\tscope_Base,\r\n\t\tscope_Handles,\r\n\t\tscope_Spectrum = opções.espectro ,\r\n\t\tscope_Values ​​= [], \r\n\t\tscope_Events = {},\r\n\t\tscope_Self;\r\n\r\n\r\n\t// Delimitar valores propostos para posições de manipulação.\r\n\tfunção getPositions ( a, b, delimit ) {\r\n\r\n\t\t// Adiciona movimento à posição atual.\r\n\t\tvar c = a + b[0], d = a + b[1];\r\n\r\n\t\t// Apenas altere a outra posição ao arrastar,\r\n\t\t// não ao deslizar padrão.\r\n\t\tif ( delimitar ) {\r\n\t\t\tif ( c < 0 ) {\r\n\t\t\t\td += Math.abs(c);\r\n\t\t\ t}\r\n\t\t\tif ( d > 100 ) {\r\n\t\t\t\tc -= ( d - 100 );\r\n\t\t\t}\ r\n\r\n\t\t\t// Limita os valores a 0 e 100.\r\n\t\t\treturn [limit(c), limit(d)];\r\n\t \t}\r\n\r\n\t\treturn [c,d];\r\n\t}\r\n\r\n\t// Forneça um evento limpo com valores de deslocamento padronizados.\ r\n\tfunction fixEvent ( e, pageOffset ) {\r\n\r\n\t\t// Evita a rolagem e panorâmica em eventos de toque, enquanto\r\n\t\t// tenta deslizar. O evento tap também depende disso. \r\n\t\te.preventDefault();\r\n\r\n\t\t// Filtre o evento para registrar o tipo, que pode ser\r\n\t\t// toque, mouse ou ponteiro. Alterações de deslocamento precisam ser\r\n\t\t// feitas com base em um evento específico.\r\n\t\tvar touch = e.type.indexOf('touch') === 0,\r\ n\t\t\tmouse = e.type.indexOf('mouse') === 0,\r\n\t\t\tpointer = e.type.indexOf('pointer') === 0,\ r\n\t\t\tx,y, event = e;\r\n\r\n\t\t// IE10 implementou eventos de ponteiro com um prefixo;\r\n\t\tif ( e.type .indexOf('MSPointer') === 0 ) {\r\n\t\t\tpointer = true;\r\n\t\t}\r\n\r\n\t\tif (toque) {\r\n\t\t\t// noUiSlider suporta um movimento de cada vez,\r\n\t\t\t// então podemos selecionar o primeiro 'changedTouch'.\r\n\t\ t\tx = e.changedTouches[0].pageX;\r\n\t\t\ty = e.changedTouches[0].pageY;\r\n\t\t}\r\n\r\n \t\tpageOffset = pageOffset || getPageOffset(); \r\n\r\n\t\tif ( mouse || ponteiro ) {\r\n\t\t\tx = e.clientX + pageOffset.x;\r\n\t\t\ty = e .clientY + pageOffset.y;\r\n\t\t}\r\n\r\n\t\tevent.pageOffset = pageOffset;\r\n\t\tevent.points = [x, y]; \r\n\t\tevent.cursor = mouse || ponteiro; // Fix #435\r\n\r\n\t\treturn event;\r\n\t}\r\n\r\n\t// Acrescenta um identificador à base.\r\n\ tfunction addHandle ( direção, índice ) {\r\n\r\n\t\tvar origin = document.createElement('div'),\r\n\t\t\thandle = document.createElement('div') ,\r\n\t\t\tclassModifier = [options.cssClasses.handleLower, options.cssClasses.handleUpper];\r\n\r\n\t\tif ( direção ) {\r\n\t\t \tclassModifier.reverse();\r\n\t\t}\r\n\r\n\t\taddClass(handle, options.cssClasses.handle);\r\n\t\taddClass(handle, classModifier [índice]);\r\n\r\n\t\taddClass(origin, options.cssClasses.origin);\r\n\t\torigin.appendChild(handle); \r\n\r\n\t\treturn origin;\r\n\t}\r\n\r\n\t// Adicione as classes de conexão apropriadas.\r\n\tfunction addConnection ( connect, target , handles ) {\r\n\r\n\t\t// Aplique as classes de conexão necessárias aos elementos\r\n\t\t// que precisam delas. Algumas classes são compostas por vários segmentos\r\n\t\t// listados na lista de classes, para permitir fácil\r\n\t\t// renomeação e fornecer um menor benefício de compactação.\r\n\ t\tswitch ( conectar ) {\r\n\t\t\tcase 1:\taddClass(target, options.cssClasses.connect);\r\n\t\t\t\t\taddClass(handles[0] , options.cssClasses.background);\r\n\t\t\t\t\tbreak;\r\n\t\t\tcase 3: addClass(handles[1], options.cssClasses.background);\ r\n\t\t\t\t\t/* falha em */\r\n\t\t\tcase 2: addClass(handles[0], options.cssClasses.connect);\r\n\ t\t\t\t\t/* falha */\r\n\t\t\tcase 0: addClass(target, options.cssClasses.background);\r\n\t\t\t\t \tbreak; \r\n\r\n\t\t\t}\r\n\r\n\t\t\t// Caso contrário, podemos simplesmente usar os valores.\r\n\t\t\treturn valores;\r\n\t\t}\r\n\t}\r\n\r\n\tfunction generateSpread (densidade, modo, grupo) {\r\n\r\n\t\tfunction safeIncrement (valor, incremento) {\r\n\t\t\t// Evite a variação de ponto flutuante eliminando as menores casas decimais.\r\n\t\t\treturn (valor + incremento).toFixed(7) / 1;\r\n\t\t}\r\n\r\n\t\tvar originalSpectrumDirection = scope_Spectrum.direction,\r\n\t\t\tindexes = {},\r\n\t\ t\tfirstInRange = scope_Spectrum.xVal[0],\r\n\t\t\tlastInRange = scope_Spectrum.xVal[scope_Spectrum.xVal.length-1],\r\n\t\t\tignoreFirst = false,\r \n\t\t\tignoreLast = false,\r\n\t\t\tprevPct = 0;\r\n\r\n\t\t// Esta função percorre o espectro de forma linear ltr,\ r\n\t\t// enquanto o método toStepping reconhece a direção. Faça-o\r\n\t\t// acreditar que é ltr. \r\n\r\n\t\t\t// Ao usar o modo 'etapas', use as etapas fornecidas.\r\n\t\t\t// Caso contrário, passaremos para a próxima subintervalo.\r\n\t\t\tif ( modo === 'steps' ) {\r\n\t\t\t\tstep = scope_Spectrum.xNumSteps[ index ];\r\n\t\t \t}\r\n\r\n\t\t\t// Padrão para uma etapa 'completa'.\r\n\t\t\tif ( !step ) {\r\n\t\t \t\tstep = high-low;\r\n\t\t\t}\r\n\r\n\t\t\t// Low pode ser 0, então teste para false. Se alto for indefinido,\r\n\t\t\t// estamos no último subintervalo. O índice 0 já foi tratado.\r\n\t\t\tif ( baixo === falso || alto === indefinido ) {\r\n\t\t\t\treturn;\r\n\t \t\t}\r\n\r\n\t\t\t// Encontre todas as etapas no subintervalo.\r\n\t\t\tfor ( i = baixo; i <= alto; i = safeIncrement(i, step) ) {\r\n\r\n\t\t\t\t// Obtém o valor percentual para o passo atual,\r\n\t\t\t\t// calcula o tamanho do subintervalo.\r\n\t\t\t\tnewPct = scope_Spectrum. q += 1 ) {\r\n\r\n\t\t\t\t\t// A proporção entre o valor arredondado e o tamanho real pode ser de ~1% de desconto.\r\n\t\ t\t\t\t// Corrige o deslocamento percentual pelo número de pontos\r\n\t\t\t\t\t// por subfaixa. densidade = 1 resultará em 100 pontos na\r\n\t\t\t\t\t// faixa completa, 2 para 50, 4 para 25, etc.\r\n\t\t\t\ t\tpctPos = prevPct + ( q * stepsize );\r\n\t\t\t\t\tindexes[pctPos.toFixed(5)] = ['x', 0];\r\n\t\ t\t\t}\r\n\r\n\t\t\t\t// Determina o tipo de ponto.\r\n\t\t\t\ttype = (group.indexOf(i) > -1) ? 1 : ( mode === 'steps' ? 2 : 0 );\r\n\r\n\t\t\t\t// Aplique a opção 'ignoreFirst' substituindo o tipo por 0.\r\ n\t\t\t\tif ( !index && ignoreFirst ) {\r\n\t\t\t\t\ttype = 0;\r\n\t\t\t\t}\r\n \r\n\t\t\t\tif ( !(i === high && ignoreLast)) {\r\n\t\t\t\t\t// Marque o 'tipo' deste ponto. 0 = simples, \r\n\t\t\tvalueOrientationClasses = [\r\n\t\t\t\toptions.cssClasses.valueHorizontal,\r\n\t\t\t\toptions.cssClasses.valueVertical\r\n\ t\t\t],\r\n\t\t\tmarkerOrientationClasses = [\r\n\t\t\t\toptions.cssClasses.markerHorizontal,\r\n\t\t\t\toptions.cssClasses .markerVertical\r\n\t\t\t];\r\n\r\n\t\taddClass(elemento, opções.cssClasses.pips);\r\n\t\taddClass(elemento, opções.ort === 0 ? options.cssClasses.pipsHorizontal : options.cssClasses.pipsVertical);\r\n\r\n\t\tfunction getClasses( type, source ){\r\n\t\t\tvar a = source === opções.cssClasses.value,\r\n\t\t\t\torientationClasses = a ? valueOrientationClasses : markerOrientationClasses,\r\n\t\t\t\tsizeClasses = a ? valueSizeClasses : markerSizeClasses;\r\n\r\n\t\t\treturn source + ' ' +orientationClasses[options.ort] + ' ' + sizeClasses[tipo]; \r\n\t\t}\r\n\r\n\t\tfunção getTags( deslocamento, fonte, valores ) {\r\n\t\t\treturn 'class=\"' + getClasses(valores [1], source) + '\" style=\"' + options.style + ': ' + offset + '%\"';\r\n\t\t}\r\n\r\n\ t\tfunction addSpread ( offset, values ​​){\r\n\r\n\t\t\tif ( scope_Spectrum.direction ) {\r\n\t\t\t\toffset = 100 - offset;\r\ n\t\t\t}\r\n\r\n\t\t\t// Aplicar a função de filtro, se estiver definida.\r\n\t\t\tvalues[1] = (valores [1] && filterFunc) ? filterFunc(values[0], values[1]) : values[1];\r\n\r\n\t\t\t// Adicionar um marcador para cada ponto\r\n\t\t\tout += '<div ' + getTags(offset, options.cssClasses.marker, values) + '></div>';\r\n\r\n\t\t\t// Os valores são acrescentados apenas para pontos marcado como '1' ou '2'.\r\n\t\t\tif ( values[1] ) {\r\n\t\t\t\tout += '<div ' + getTags(offset, options.cssClasses.value, values) + '>' + formatter.to(values[0]) + '</div>';\r\n\t\t\t}\r\n \t\t}\r\n\r\n\t\t// Acrescentar todos os pontos.\r\n\t\tObject.keys(spread).forEach(function(a){\r\n\t \t\taddSpread(a, spread[a]);\r\n\t\t});\r\n\r\n\t\telement.innerHTML = out;\r\n\r\n\ elemento t\treturn;\r\n\t}\r\n\r\n\tfunção pips ( grid ) {\r\n\r\n\tvar mode = grid.mode,\r\n\t\ tdensity = grid.density || 1,\r\n\t\tfilter = grid.filter || false,\r\n\t\tvalores = grade.valores || false,\r\n\t\tstepped = grid.stepped || false,\r\n\t\tgroup = getGroup( modo, valores, escalonado ),\r\n\t\tspread = generateSpread( densidade, modo, grupo ),\r\n\t\tformat = grid.format || {\r\n\t\t\tto: Math.round\r\n\t\t};\r\n\r\n\t\treturn scope_Target.appendChild(addMarking(\r\n\t\ t\tspread,\r\n\t\t\tfilter,\r\n\t\t\tformat\r\n\t\t)); \r\n\t}\r\n\r\n\r\n\t// Abreviatura para dimensões de base.\r\n\tfunction baseSize ( ) {\r\n\t\tvar rect = scope_Base. getBoundingClientRect(), alt = 'offset' + ['Width', 'Height'][options.ort];\r\n\t\treturn options.ort === 0 ? (rect.width||scope_Base[alt]) : (rect.height||scope_Base[alt]);\r\n\t}\r\n\r\n\t// Tratamento de evento externo\r\n \tfunction fireEvent ( event, handleNumber, tap ) {\r\n\r\n\t\tvar i;\r\n\r\n\t\t// Durante a inicialização, não dispara eventos.\r\ n\t\tfor ( i = 0; i < options.handles; i++ ) {\r\n\t\t\tif ( scope_Locations[i] === -1 ) {\r\n\t\t\ t\treturn;\r\n\t\t\t}\r\n\t\t}\r\n\r\n\t\tif ( handleNumber !== indefinido && opções.handles !== 1 ) {\r\n\t\t\thandleNumber = Math.abs(handleNumber - options.dir);\r\n\t\t}\r\n\r\n\t\tObject.keys(scope_Events) .) === -1 && event.buttons === 0 && data.buttonsProperty !== 0 ) {\r\n\t\t\treturn end(event, data);\r\n\t\t} \r\n\r\n\t\tvar handles = data.handles || scope_Handles, posições, estado = false,\r\n\t\t\tproposta = ((event.calcPoint - data.start) * 100) / data.baseSize,\r\n\t\t\thandleNumber = handles[ 0] === scope_Handles[0] ? 0 : 1, i;\r\n\r\n\t\t// Calcula posições relativas para as alças.\r\n\t\tpositions = getPositions( proposta, data.positions, alças.length > 1) ;\r\n\r\n\t\tstate = setHandle ( handles[0], posições[handleNumber], handles.length === 1 );\r\n\r\n\t\tif ( handles. comprimento > 1 ) {\r\n\r\n\t\t\tstate = setHandle ( handles[1], posições[handleNumber?0:1], false ) || estado;\r\n\r\n\t\t\tif ( estado ) {\r\n\t\t\t\t// disparar para ambos os manipuladores\r\n\t\t\t\tfor ( i = 0; i < data.handles.length; i++ ) {\r\n\t\t\t\t\tfireEvent('slide', i);\r\n\t\t\t\t}\r\n\t\t\t}\ r\n\t\t} else if ( estado ) {\r\n\t\t\t// Disparar para um único handle\r\n\t\t\tfireEvent('slide', handleNumber);\ r\n\t\t}\r\n\t}\r\n\r\n\t// Desvincular mover eventos no documento, chamar retornos de chamada.\r\n\tfunction end ( event, data ) {\ r\n\r\n\t\t// O identificador não está mais ativo, então remova a classe.\r\n\t\tvar active = scope_Base.querySelector( '.' + options.cssClasses.active ), \r\n\t\t\thandleNumber = data.handles[0] === scope_Handles[0] ? 0 : 1;\r\n\r\n\t\tif ( ativo !== nulo ) {\r\n\t\t\tremoveClass(ativo, opções.cssClasses.ativo);\r\n\t \t}\r\n\r\n\t\t// Remove estilos de cursor e eventos de seleção de texto vinculados ao corpo.\r\n\t\tif ( event.cursor ) {\r\n\t \t\tdocument.body.style.cursor = '';\r\n\t\t\tdocument.body.removeEventListener('selectstart', \r\n\t\t}\r\n\t}\r\n\r\n\t// Mover a alça mais próxima para o local tocado.\r\n\tfunção toque ( evento ) {\r\n \r\n\t\tvar location = event.calcPoint, total = 0, handleNumber, to;\r\n\r\n\t\t// O evento tap não deve se propagar para cima e causar 'edge' para run.\r\n\t\tevent.stopPropagation();\r\n\r\n\t\t// Some os deslocamentos do identificador.\r\n\t\tscope_Handles.forEach(function(a) {\r\n\t\t\ttotal += offset(a)[opções.estilo];\r\n\t\t});\r\n\r\n\t\t// Encontre o identificador mais próximo da posição tocada.\r\n\t\thandleNumber = ( location < total/2 || scope_Handles.length === 1 ) ? 0 : 1;\r\n\r\n\t\t// Verifique se o manipulador não está desabilitado se sim defina o número para o próximo manipulador\r\n\t\tif (scope_Handles[handleNumber].hasAttribute('disabled ')) {\r\n\t\t\thandleNumber = handleNumber ? 0 : 1;\r\n\t\t}\r\n\r\n\t\tlocation -= offset(scope_Base)[opções.estilo]; \r\n\r\n\t\t// Calcula a nova posição.\r\n\t\tto = ( location * 100 ) / baseSize();\r\n\r\n\t\tif ( !options.events.snap ) {\r\n\t\t\t// Sinalizar o controle deslizante, pois ele está agora em um estado de transição.\r\n\t\t\t// A transição leva um valor configurável de ms (padrão 300). Reative o controle deslizante depois disso.\r\n\t\t\taddClassFor(scope_Target, options.cssClasses.tap, options.animationDuration );\r\n\t\t}\r\n\r\n\ t\t// Suporta manipuladores 'desativados'\r\n\t\tif ( scope_Handles[handleNumber].hasAttribute('desativado') ) {\r\n\t\t\treturn false;\r\n\t \t}\r\n\r\n\t\t// Encontre a alça mais próxima e calcule o ponto de toque.\r\n\t\t// A alça definida para a nova posição.\r\n\ t\tsetHandle(scope_Handles[handleNumber], to );\r\n\r\n\t\tfireEvent('slide', handleNumber, true);\r\n\t\tfireEvent('set', handleNumber, true );\r\n\t\tfireEvent(' \r\n\t\t}\r\n\r\n\t\t// Tornar o intervalo arrastável.\r\n\t\tif ( behavior.drag ){\r\n\r\n \t\t\tvar arrastar = [scope_Base.querySelector( '.' + opções.cssClasses.connect )];\r\n\t\t\taddClass(arrastar[0], opções.cssClasses.draggable);\r \n\r\n\t\t\t// Quando o intervalo é fixo, todo o intervalo pode\r\n\t\t\t// ser arrastado pelas alças. O identificador na primeira\r\n\t\t\t// origem irá propagar o evento inicial para cima,\r\n\t\t\t// mas precisa ser vinculado manualmente na outra.\r \n\t\t\tif ( behavior.fixed ) {\r\n\t\t\t\tdrag.push(scope_Handles[(drag[0] === scope_Handles[0] ? 1 : 0)]. filhos[0]);\r\n\t\t\t}\r\n\r\n\t\t\tdrag.forEach(função( elemento ) {\r\n\t\t\t\ tattach (actions.start, elemento, start, {\r\n\t\t\t\t\thandles: scope_Handles\r\n\t\t\t\t});\r\n\t\t \t});\r\n\t\t}\r\n\t}\r\n\r\n\r\n\t// Testar valores sugeridos e aplicar margem, passo. \r\n\tfunction setHandle ( handle, to, noLimitOption ) {\r\n\r\n\t\tvar trigger = handle !== scope_Handles[0] ? 1: 0,\r\n\t\t\tlowerMargin = scope_Locations[0] + options.margin,\r\n\t\t\tupperMargin = scope_Locations[1] - options.margin,\r\n\t \t\tlowerLimit = scope_Locations[0] + options.limit,\r\n\t\t\tupperLimit = scope_Locations[1] - options.limit;\r\n\r\n\t\t// Para controles deslizantes com várias alças,\r\n\t\t// limite o movimento para a outra alça.\r\n\t\t// Aplique a opção de margem adicionando-a às posições da alça.\r\n\t\ tif (scope_Handles.length > 1) {\r\n\t\t\tto = gatilho? Math.max( to, lowerMargin ) : Math.min( to, upperMargin );\r\n\t\t}\r\n\r\n\t\t// A opção de limite tem o efeito oposto, limitando alças a uma\r\n\t\t// distância máxima uma da outra. O limite deve ser > 0, caso contrário, as alças\r\n\t\t// não poderiam ser movidas. ' noLimitOption' é definido como 'false'\r\n\t\t// para o método .val(), exceto para passagem 4/4.\r\n\t\tif ( noLimitOption !== false && opções. limit && scope_Handles.length > 1 ) {\r\n\t\t\tto = trigger ? Math.min ( to, lowerLimit ) : Math.max( to, upperLimit );\r\n\t\t}\r\n\r\n\t\t// Lida com a opção de etapa.\r\n \t\tto = scope_Spectrum.getStep( to );\r\n\r\n\t\t// Limite de porcentagem para o intervalo de 0 a 100\r\n\t\tto = limit(to);\r \n\r\n\t\t// Retorna falso se o identificador não puder se mover\r\n\t\tif ( para === scope_Locations[trigger] ) {\r\n\t\t\treturn false ;\r\n\t\t}\r\n\r\n\t\t// Defina a alça para a nova posição.\r\n\t\t// Use requestAnimationFrame para pintura eficiente.\r \n\t\t// Sem efeito significativo no Chrome, o Edge apresenta melhorias dramáticas\r\n\t\t// de desempenho.\r\n\t\tif ( window.requestAnimationFrame ) {\r\n\t \t\twindow. \r\n\t\t}\r\n\r\n\t\t// A animação é opcional.\r\n\t\t// Certifique-se de que os valores iniciais foram definidos antes de usar o posicionamento animado.\ r\n\t\tif (options.animate && scope_Locations[0] !== -1) {\r\n\t\t\taddClassFor(scope_Target, options.cssClasses.tap, options.animationDuration);\r\ n\t\t}\r\n\r\n\t\t// Determina com que frequência definir os identificadores.\r\n\t\tcount = scope_Handles.length > 1 ? 3: 1;\r\n\r\n\t\tif ( valores.comprimento === 1 ) {\r\n\t\t\tcontagem = 1;\r\n\t\t}\r \n\r\n\t\tsetValues ​​( contagem, valores );\r\n\r\n\t\t// Aciona o evento 'set' para ambos os manipuladores.\r\n\t\tfor ( i = 0; i < scope_Handles.length; i++ ) {\r\n\r\n\t\t\t// Dispara o evento apenas para os handles que receberam um novo valor, conforme #579\r\n\t \t\tif ( values[i] !== null && fireSetEvent ) {\r\n\t\t\t\tfireEvent('set', i); \r\n\t\t\t}\r\n\t\t}\r\n\t}\r\n\r\n\t// Obtém o valor do controle deslizante.\r\n\tfunção valueGet ( ) {\r\n\r\n\t\tvar i, retour = [];\r\n\r\n\t\t// Obtém o valor de todos os handles.\r\n\t \tfor ( i = 0; i < options.handles; i += 1 ){\r\n\t\t\tretour[i] = options.format.to( scope_Values[i] );\r\n\ t\t}\r\n\r\n\t\treturn inSliderOrder( retorno );\r\n\t}\r\n\r\n\t// Remove classes da raiz e a esvazia.\ r\n\tfunction destroy ( ) {\r\n\r\n\t\tfor ( var key in options.cssClasses ) {\r\n\t\t\tif ( !options.cssClasses.hasOwnProperty(key) ) { continuar; }\r\n\t\t\tremoveClass(scope_Target, options.cssClasses[key]);\r\n\t\t}\r\n\r\n\t\twhile (scope_Target.firstChild) {\ r\n\t\t\tscope_Target.removeChild(scope_Target.firstChild);\r\n\t\t}\r\n\r\n\t\tdelete scope_Target.noUiSlider;\r\n\t}\ r\n\r\n\t// Obtém o tamanho do passo atual para o controle deslizante. \r\n\r\n\t\t\t\t// Descer 'um degrau' pode colocar o controle deslizante em um subintervalo diferente, então nós\r\n\t\t\t\t/ / precisa alternar entre a etapa atual ou a anterior.\r\n\t\t\t\tprev = Number((value - step[2]).toFixed(stepDecimals)),\r\n\r\n \t\t\t\t// Se o valor corresponder ao passo, retorna o valor do passo atual. Caso contrário, use a etapa\r\n\t\t\t\t// anterior. Retorna nulo se o controle deslizante estiver em seu valor mínimo.\r\n\t\t\t\tdecrement = location === 0 ? null : (anterior >= etapa[1]) ? passo[2] : (passo[0] || false);\r\n\r\n\t\t\treturn [decremento, incremento];\r\n\t\t});\r\n \r\n\t\t// Retorna valores na ordem correta.\r\n\t\treturn inSliderOrder( retour );\r\n\t}\r\n\r\n\t// Anexar um evento para este controle deslizante, possivelmente incluindo um namespace\r\n\tfunção bindEvent ( namespacedEvent, callback ) {\r\n\t\tscope_Events[namespacedEvent] = scope_Events[namespacedEvent] || [];\r\n\t\tscope_Events[namespacedEvent].push(callback);\r\n\r\n\t\t// Se o limite do evento for 'atualizar', dispare-o imediatamente para todos os manipuladores. \r\n\t\tif ( namespacedEvent.split('.')[0] === 'update' ) {\r\n\t\t\tscope_Handles.forEach(function(a, index){\r \n\t\t\t\tfireEvent('atualizar', índice);\r\n\t\t\t});\r\n\t\t}\r\n\t}\r\ n\r\n\t// Desfazer anexo de event\r\n\tfunction removeEvent ( namespacedEvent ) {\r\n\r\n\t\tvar event = namespacedEvent && namespacedEvent.split('.')[0 ],\r\n\t\t\tnamespace = event && namespacedEvent.substring(event.length);\r\n\r\n\t\tObject.keys(scope_Events).forEach(function( bind ){\ r\n\r\n\t\t\tvar tEvent = bind.split('.')[0],\r\n\t\t\t\ttNamespace = bind.substring(tEvent.length); optionsToUpdate.range,\r\n\t\t\tanimate: optionsToUpdate.animate,\r\n\t\t\tsnap: optionsToUpdate.snap === indefinido ? options.snap : optionsToUpdate.snap\r\n\t\t});\r\n\r\n\t\t['margin', 'limit', 'range', 'animate'].forEach( function(name){\r\n\r\n\t\t\t// Apenas altere as opções que realmente passamos para atualizar.\r\n\t\t\tif ( optionsToUpdate[name] != = indefinido ) {\r\n\t\t\t\toptions[nome] = opçõesParaAtualizar[nome];\r\n\t\t\t}\r\n\t\t});\r\ n\r\n\t\t// Salva a direção atual do espectro como testOptions na chamada testRange\r\n\t\t// não depende da direção atual\r\n\t\tnewOptions.spectrum.direction = scope_Spectrum.direction;\r\n\t\tscope_Spectrum = newOptions.spectrum;\r\n\r\n\t\t// Invalida o posicionamento atual para que valueSet force uma atualização.\r\n\t\tscope_Locations = [-1, -1];\r\n\t\tvalueSet(optionsToUpdate. iniciar || v, fireSetEvent);\r\n\t}\r\n\r\n\r\n\t// Lança um erro se o controle deslizante já foi inicializado.\r\n\tif ( scope_Target.noUiSlider ) { \r\n\t\tthrow new Error('Slider já foi inicializado.');\r\n\t}\r\n\r\n\t// Crie o elemento base, inicialize o HTML e configure as classes. \r\n\t// Adicionar identificadores e links.\r\n\tscope_Base = addSlider(options.dir, options.ort, scope_Target);\r\n\tscope_Handles = addHandles(options.handles, options.dir, scope_Base );\r\n\r\n\t// Defina as classes de conexão.\r\n\taddConnection ( options.connect, scope_Target, scope_Handles );\r\n\r\n\tif ( options.pips ) {\r\n\t\tpips(options.pips);\r\n\t}\r\n\r\n\tif (options.tooltips) {\r\n\t\ttooltips(); \r\n\t}\r\n\r\n\tscope_Self = {\r\n\t\tdestroy: destruir,\r\n\t\tsteps: getCurrentStep,\r\n\t\ton: bindEvent,\r\n\t\toff: removeEvent,\r\n\t\tget: valueGet,\r\n\t\tset: DECORRENTE DE OU EM CONEXÃO COM O SOFTWARE OU O\n// USO OU OUTROS NEGÓCIOS NO SOFTWARE.\n\n// resolve . e .. elementos em uma matriz de caminho com nomes de diretório\n// não deve haver barras, elementos vazios ou nomes de dispositivos (c:\\) na matriz\n// (portanto, também não há barras iniciais e finais - é não distingue\n// caminhos relativos e absolutos)\nfunction normalizeArray(parts, allowAboveRoot) {\n // se o caminho tentar ir acima da raiz, `up` acaba > 0\n var up = 0;\ n for (var i = parts.length - 1; i >= 0; i--) {\n var last = parts[i];\n if (last === '.') {\n parts.splice (i, 1);\n } else if (last === '..') {\n parts.splice(i, 1);\n up++;\n } else if (up) {\n parts. splice(i, 1);\n up--;\n }\n }\n\n // se for permitido que o caminho vá acima da raiz, restaure .. s\n if (allowAboveRoot) {\n for (; up--; up) {\n parts.unshift('..');\n }\n }\n\n return parts;\n}\n \n// caminho.resolve([de ...], para)\n// versão posix\nexports.resolve = function() {\n var resolvidoPath = '',\n resolvidoAbsoluto = false;\n\n for (var i = arguments.length - 1; i >= -1 && !resolvedAbsolute; i--) {\n var path = (i >= 0) ? argumentos[i] : process.cwd();\n\n // Ignora entradas vazias e inválidas\n if (typeof path !== 'string') {\n throw new TypeError('Arguments to path.resolve must be strings');\n } else if (!caminho) {\n continue;\n }\n\n resolvidoPath = caminho + '/' + resolvidoPath;\n resolvidoAbsolute = caminho.charAt(0) === '/ ';\n }\n\n // Neste ponto, o caminho deve ser resolvido para um caminho absoluto completo, mas\n // manipula caminhos relativos para serem seguros (pode acontecer quando process.cwd() falha)\n\n // Normaliza o caminho\n solvePath = normalizeArray(filter(resolvedPath.split('/'), function( p) {\n return !!p;\n }), !resolvedAbsolute).join('/');\n\n return ((resolvedAbsolute ? '/' : '') + solvePath) || '.';\n};\n\n// path.normalize(path)\n// versão posix\nexports.normalize = function(path) {\n var isAbsolute = exports.isAbsolute(path),\n trailingSlash = substr(path, -1) === '/';\n\n // Normaliza o caminho\n path = normalizeArray(filter(path.split('/'), function(p) {\n return !!p;\n }), !isAbsolute).join('/');\n\n if (!path && !isAbsolute) {\n path = '.';\n }\n if (path && trailingSlash) {\n caminho += '/'; dirname = function (path) {\n if (typeof path !== 'string') path = path + '';\n if (path.length === 0) return '.';\n var code = path .charCodeAt(0);\n var hasRoot = code === 47 /*/*/;\n var end = -1;\n var matchedSlash = true;\n for (var i = path.length - 1; i >= 1; --i) {\n code = path.charCodeAt(i);\n if (code === 47 /*/*/) {\n if (!matchedSlash) {\n end = i ;\n break;\n }\n } else {\n // Vimos o primeiro separador sem caminho\n matchedSlash = false;\n }\n }\n\n if (end === -1) retorna hasRoot ? '/' : '.';\n if (hasRoot && end === 1) {\n // return '//';\n // correção de compatibilidade com versões anteriores:\n return '/';\n } \n return path.slice(0, end);\n};\n\nfunction basename(path) {\n if (typeof path !== 'string' \n preDotState === 1 && startDot === end - 1 && startDot === startPart + 1) {\n return '';\n }\n return path.slice(startDot, end);\n}; \n\nfunção filtro (xs, f) {\n if (xs.filter) return xs.filter(f);\n var res = [];\n for (var i = 0; i < xs.length; i++) {\n if (f(xs[i], i, xs)) res.push(xs[i]);\n }\n return res;\n}\n\n// String.prototype. substr - índice negativo não funciona no IE8\nvar substr = 'ab'.substr(-1) === 'b'\n ? function (str, start, len) { return str.substr(start, len) }\n : function (str, start, len) {\n if (start < 0) start = str.length + start;\n return str.substr(start, len);\n }\n;\n\n/* WEBPACK VAR INJECTION */}.call(this, __webpack_require__(/*! ./../process/browser.js */ \ "./node_modules/process/browser.js\" \n\n// v8 gosta de objetos previsíveis\nfunção Item(divertido, array) {\n this.fun = divertido;\n this.array = array;\n}\nItem.prototype.run = function () {\ n this.fun.apply(null, this.array);\n};\nprocess.title = 'browser';\nprocess.browser = true;\nprocess.env = {};\nprocess.argv = []; \nprocesso.versão = ''; // string vazia para evitar problemas de regexp\nprocess.versions = {};\n\nfunction noop() {}\n\nprocess.on = noop;\nprocess.addListener = noop;\nprocess.once = noop;\nprocess .off = noop;\nprocess.removeListener = noop;\nprocess.removeAllListeners = noop;\nprocess.emit = noop;\nprocess.prependListener = noop;\nprocess.prependOnceListener = noop;\n\nprocess.listeners = função (nome ) { return [] }\n\nprocess.binding = function (name) {\n throw new Error('process.binding não é suportado');\n};\n\nprocess. cwd = function () { return '/' };\nprocess.chdir = function (dir) {\n lançar novo erro('process.chdir não é suportado');\n};\nprocess.umask = function() { retorna 0; };\n\n\n//# sourceURL=webpack://VLibras/./node_modules/process/browser.js?")},"./node_modules/style-loader/dist/runtime/injectStylesIntoStyleTag.js" :function(module,exports,__webpack_require__){"use strict";eval("\n\nvar stylesInDom = {};\n\nvar isOldIE = function isOldIE() {\n var memo;\n return function memorize() {\n if (typeof memo === 'undefined') {\n // Teste para IE <= 9 conforme proposto por Browserhacks\n // @see http://browserhacks. com/#hack-e71d8692f65334173fee715c222cb805\n // Testa a existência de globais padrão para permitir que o style-loader\n // opere corretamente em ambientes fora do padrão\n // @consulte https://github.com/webpack- contrib/style-loader/issues/177\n memo = Boolean(window && document && document.all && !window.atob);\n }\n\n return memo;\n };\n}();\ n\nvar getTarget = function getTarget() {\n var memo = {};\n return function memorize(target) {\n if (typeof memo[target] === 'undefined') {\n var styleTarget = document .querySelector(alvo); // Caso especial para retornar o cabeçalho do iframe em vez do próprio iframe\n\n if (window.HTMLIFrameElement && styleTarget instanceof window. HTMLIFrameElement) {\n try {\n // Isso lançará uma exceção se o acesso ao iframe for bloqueado\n // devido a restrições de origem cruzada\n styleTarget = styleTarget.contentDocument.head;\n } catch (e) { \n // istambul ignora próximo\n styleTarget = null;\n }\n }\n\n memo[target] = styleTarget;\n }\n\n return memo[target];\n };\n} ();\n\nfunction listToStyles(list, options) {\n var styles = [];\n var newStyles = {};\n\n for (var i = 0; i < list.length; i++) { \n var item = lista[i];\n var id = opções.base ? item[0] + opções.base : item[0];\n var css = item[1];\n var media = item[2];\n var sourceMap = item[3];\n var part = { \n css: css,\n media: media,\n sourceMap: sourceMap\n };\n\n if (!newStyles[id]) {\n styles. partes\n };\n }\n }\n}\n\nfunção insertStyleElement(options) {\n var style = document.createElement('style');\n\n if (typeof options.attributes.nonce = == 'indefinido') {\n var nonce = true ? __webpack_require__.nc : undefined;\n\n if (nonce) {\n options.attributes.nonce = nonce;\n }\n }\n\n Object.keys(options.attributes).forEach(function (key) {\n style.setAttribute(key, options.attributes[key]);\n });\n\n if (typeof options.insert === 'function') {\n options.insert(style);\ n } else {\n var target = getTarget(options.insert || 'head');\n\n if (!target) {\n throw new Error(\"Não foi possível encontrar um destino de estilo. Isso provavelmente significa que o valor do parâmetro 'inserir' é inválido.\");\n }\n\n target.appendChild(estilo);\n }\n\n estilo de retorno; \n}\n\nfunction removeStyleElement(style) {\n // istambul ignora if\n if (style.parentNode === null) {\n return false;\n }\n\n style.parentNode.removeChild( estilo);\n}\n/* istambul ignora próximo */\n\n\nvar replaceText = function replaceText() {\n var textStore = [];\n return function replace(índice, substituição) {\n textStore [index] = substituição;\n return textStore.filter(Boolean).join('\\n');\n };\n}();\n\nfunction applyToSingletonTag(estilo, índice, remover, obj) { \n var css = remover ? '' : obj.css; // Para IE antigo\n\n /* istambul ignore if */\n\n if (style.styleSheet) {\n style.styleSheet.cssText = replaceText(index, css);\n } else {\n var cssNode = document.createTextNode(css);\n var childNodes = style.childNodes;\n\n if (childNodes[index]) {\n style.removeChild(childNodes[index]); \n }\n}\n\nvar singleton = null;\nvar singletonCounter = 0;\n\nfunction addStyle(obj, options) {\n var style;\n var update;\n var remove;\n\n if (options.singleton) {\n var styleIndex = singletonCounter++;\n style = singleton || (singleton = insertStyleElement(options));\n update = applyToSingletonTag.bind(null, style, styleIndex, false);\n remove = applyToSingletonTag.bind(null, style, styleIndex, true);\n } else {\n style = insertStyleElement(options);\n update = applyToTag.bind(null, style, options);\n\n remove = function remove() {\n removeStyleElement(style);\n };\n }\n\ n update(obj);\n função de retorno updateStyle(newObj) {\n if (newObj) {\n if (newObj.css === obj.css && newObj.media === obj.media && newObj.sourceMap = == obj.sourceMap) {\n return; \n }\n\n update(obj = newObj);\n } else {\n remove();\n }\n };\n}\n\nmodule.exports = função (lista, opções) {\ n opções = opções || {};\n options.attributes = typeof options.attributes === 'objeto' ? opções.atributos : {}; // Força a solução de tag única no IE6-9, que tem um limite rígido no número de tags <style>\n // que permitirá em uma página\n\n if (!options.singleton && typeof options.singleton !== 'boolean') {\n options.singleton = isOldIE();\n }\n\n var styles = listToStyles(list, options);\n addStylesToDom(styles, options);\n return function update( newList) {\n var mayRemove = [];\n\n for (var i = 0; i < styles.length; i++) {\n var item = styles[i];\n var domStyle = stylesInDom[item. id];\n\n if (domStyle) {\n domStyle.refs--;\n mayRemove. fontWeight',\n 'fontStretch',\n 'fontSize',\n 'fontSizeAdjust',\n 'lineHeight',\n 'fontFamily',\n\n 'textAlign',\n 'textTransform',\n ' textIndent',\n 'textDecoration', // pode não fazer diferença, mas é melhor ser seguro\n\n 'letterSpacing',\n 'wordSpacing',\n\n 'tabSize',\n 'MozTabSize'\n \n];\n\nvar isBrowser = (tipo de janela !== 'indefinido');\nvar isFirefox = (isBrowser && window.mozInnerScreenX != null);\n\nfunction getCaretCoordinates(elemento, posição, opções) {\ n if (!isBrowser) {\n throw new Error('textarea-caret-position#getCaretCoordinates só deve ser chamado em um navegador');\n }\n\n var debug = opções && opções.debug || false;\n if (depurar) {\n var el = document. querySelector('#input-textarea-caret-position-mirror-div');\n if (el) el.parentNode.removeChild(el);\n }\n\n // O espelho div irá replicar o estilo da área de texto \n var div = document.createElement('div');\n div.id = 'input-textarea-caret-position-mirror-div';\n document.body.appendChild(div);\n\n var style = div.style;\n var computered = window.getComputedStyle ? window.getComputedStyle(element) : element.currentStyle; // currentStyle para IE < 9\n var isInput = element.nodeName === 'INPUT';\n\n // Estilos padrão da área de texto\n style.whiteSpace = 'pre-wrap';\n if (!isInput) \n style.wordWrap = 'quebra-palavra'; // somente para textarea-s\n\n // Posição fora da tela\n style.position = 'absolute'; // necessário para retornar as coordenadas apropriadamente\n if (!debug)\n style. visibilidade = 'oculto'; // não 'display: none' porque queremos renderização\n\n // Transfere as propriedades do elemento para o div\n properties.forEach(function (prop) {\n if (isInput && prop === 'lineHeight') {\n // Caso especial para <input>s porque o texto é renderizado centralizado e a altura da linha pode ser != altura\n style.lineHeight = computed.height;\n } else {\n style[prop] = computed[prop ];\n }\n });\n\n if (isFirefox) {\n // Firefox mente sobre a propriedade overflow para textareas: https://bugzilla.mozilla.org/show_bug.cgi?id=984275\ n if (element.scrollHeight > parseInt(computed.height))\n style.overflowY = 'scroll';\n } else {\n style.overflow = 'hidden'; // para o Chrome não renderizar uma barra de rolagem; IE mantém overflowY = 'scroll'\n }\n\n div. textContent = element.value.substring(0, position);\n // O segundo tratamento especial para input type=\"text\" vs textarea:\n // os espaços precisam ser substituídos por espaços sem quebra - http: //stackoverflow.com/a/13402035/1269037\n if (isInput)\n div.textContent = div.textContent.replace(/\\s/g, '\\u00a0');\n\n var span = document.createElement('span');\n // A quebra deve ser replicada *exatamente*, inclusive quando uma palavra longa chega\n // na próxima linha, com espaço em branco no final da linha anterior (#7). \n // A *única* maneira confiável de fazer isso é copiar todo o restante do conteúdo\n // da área de texto no <span> criado na posição do cursor.\n // Para entradas, apenas '. ' seria o suficiente, mas não precisa se preocupar.\n span.textContent = element.value.substring(position) || '.'; // || porque um span falso completamente vazio não é renderizado\n div.appendChild(span);\n\n var coordena = {\n top: span.offsetTop + parseInt(computed['borderTopWidth']),\n left : span.offsetLeft + parseInt(computed['borderLeftWidth']),\n height: parseInt(computed['lineHeight'])\n };\n\n if (debug) {\n span.style.backgroundColor = ' #aaa';\n } else {\n document.body.removeChild(div);\n }\n\n retornar coordenadas;\n}\n\nif ( true && typeof module.exports != 'indefinido') {\n module.exports = getCaretCoordinates;\n} else if(isBrowser) {\n window.getCaretCoordinates = getCaretCoordinates;\n}\n\n}());\n\n\n//# sourceURL=webpack ://VLibras/./node_modules/textarea-caret/index.js?")},"./node_modules/trie-search/index.js":function(module,exports, /[Ã¹ÃºÃ»Ã¼]/ig,\n suplente: 'u'\n },\n {\n regex: /[Ã¦]/ig,\n suplente: 'ae'\n }\n];\ n\nString.prototype.replaceCharAt=função(índice, substituição) {\n return this.substr(0, índice) + substituição + this.substr(índice + substituição.comprimento);\n};\n\nvar TrieSearch = function (keyFields, opções) {\n this.options = opções || {};\n\n // Padrão ignoreCase para true\n this.options.ignoreCase = (this.options.ignoreCase === indefinido) ? true : this.options.ignoreCase;\n this.options.maxCacheSize = this.options.maxCacheSize || MAX_CACHE_SIZE;\n this.options.cache = this.options.hasOwnProperty('cache') ? this.options.cache : true;\n this.options.splitOnRegEx = this.options.hasOwnProperty('splitOnRegEx') ? this.options.splitOnRegEx : /\\s/g;\n this.options.splitOnGetRegEx = this.options.hasOwnProperty(' splitOnGetRegEx') ? this.options.splitOnGetRegEx : this.options.splitOnRegEx;\n this.options.min = this.options.min || 1;\n this.options.keepAll = this.options.hasOwnProperty('keepAll') ? this.options.keepAll : false;\n this.options.keepAllKey = this.options.hasOwnProperty('keepAllKey') ? this.options.keepAllKey : 'id';\n this.options.idFieldOrFunction = this.options.hasOwnProperty('idFieldOrFunction') ? this.options.idFieldOrFunction : indefinido;\n this.options.expandRegexes = this.options.expandRegexes || DEFAULT_INTERNATIONALIZE_EXPAND_REGEXES;\n this.options.insertFullUnsplitKey = this.options.hasOwnProperty('insertFullUnsplitKey') ? this.options.insertFullUnsplitKey : false;\n\n this.keyFields = keyFields ? (keyFields instanceof Array ? keyFields : [keyFields]) : [];\n this.root = {}; \n this.size = 0;\n\n if (this.options.cache) {\n this.getCache = new HashArray('key');\n }\n};\n\nfunction deepLookup(obj, keys) {\n return keys.length === 1 ? obj[keys[0]] : deepLookup(obj[keys[0]], keys.slice(1, keys.length));\n}\n\nTrieSearch.prototype = {\n add: function (obj, customKeys ) {\n if (this.options.cache)\n this.clearCache();\n\n // Alguém pode ter chamado add através de um array forEach onde o segundo parâmetro é um número\n if (typeof customKeys == = 'número') {\n customKeys = indefinido;\n }\n\n var keyFields = customKeys || this.keyFields;\n\n for (var k in keyFields)\n {\n var key = keyFields[k],\n isKeyArr = instância de chave de Array,\n val = isKeyArr ? deepLookup(obj, key) : obj[key];\n\n if (!val) continue;\n\n val = val.toString(); \n\n var expandValues ​​= this.expandString(val);\n\n for (var v = 0; v < expandValues.length; v++) {\n var expandValue = expandValues[v];\n\n this. map(expandedValue, obj);\n }\n }\n },\n /**\n * Por padrão, usando o options.expandRegexes, dada uma string como 'Ã¶ Ã¤r bra', isso irá expandi-lo para:\n *\n * ['Ã¶ Ã¤r bra', 'o Ã¤r bra', 'Ã¶ ar bra', 'o ar bra']\n *\n * Por padrão, isso foi criado para permitir a internacionalização, mas também pode ser expandido para\n * permitir alternativas de palavras, etc. como alternativas de ortografia ('teh' e 'the').\n *\n * Isso é usado para inserção! Isso não deve ser usado para pesquisa, pois se uma pessoa digitar explicitamente\n * 'Ã¤', ela provavelmente não deseja ver todos os resultados para 'a'. \n clearCache: function () {\n // if (this.getCache && !this.getCache._list.length) {\n // return;\n // }\n this.getCache = new HashArray('chave ');\n },\n cleanCache: function () {\n while (this.getCache.all.length > this.options.maxCacheSize)\n this.getCache.remove(this.getCache.all[0]) ;\n },\n addFromObject: function (obj, valueField) {\n if (this.options.cache)\n this.clearCache();\n\n valueField = valueField || 'valor';\n\n if (this.keyFields.indexOf('_key_') == -1)\n this.keyFields.push('_key_');\n\n for (var chave em obj)\ n {\n var o = {_key_: key};\no[valueField] = obj[key];\n this.add(o);\n }\n },\n map: function (key, value) {\n if (this.options.splitOnRegEx && this.options.splitOnRegEx. \n }\n }\n }\n\n if (this.options.cache)\n this.clearCache();\n\n if (this.options.keepAll) {\n this.indexed = this. indexado || new HashArray([this.options.keepAllKey]);\n this.indexed.add(value);\n }\n\n if (this.options.ignoreCase) {\n key = key.toLowerCase();\ n }\n\n var keyArr = this.keyToArr(key),\n self = this;\n\n insert(keyArr, value, this.root);\n\n function insert(keyArr, value, node) {\n if (keyArr.length == 0)\n {\n node['value'] = node['value'] || [];\n nó['valor'].push(valor);\n retorno; \n }\n\n var k = keyArr.shift();\n\n if (!node[k])\n self.size++;\n\n node[k] = node[k] || {};\n\n insert(keyArr, value, node[k])\n }\n },\n keyToArr: function (key) {\n var keyArr; + limite\n }\n return cacheKey\n },\n _get: function (frase, limite) {\n frase = this.options.ignoreCase ? frase.toLowerCase() : frase;\n \n var c, nó;\n if (this.options.cache && (c = this.getCache.get(this._getCacheKey(frase, limite))))\n return c.value;\n\n var ret = indefinido,\n haKeyFields = this.options.indexField ? [this.options.indexField] : this.keyFields,\n palavras = this.options.splitOnGetRegEx ? frase.split(this.options.splitOnGetRegEx) : [frase];\n\n for (var w = 0, l = words.length; w < l; w++)\n {\n if (this.options.min && words[w].length < this.options.min)\n continue;\n\n var temp = new HashArray(haKeyFields);\n\n if (node ​​= this.findNode(words[w]))\ n agregado(nó, temp);\n\n ret = ret ? ret.intersection(temp): temp; \n }\n \n var v = ret ? ret.all : [];\n\n if (this.options.cache)\n {\n var cacheKey = this._getCacheKey(frase, limite)\n this.getCache.add({chave: cacheKey, valor: v});\n this.cleanCache();\n }\n\n return v;\n \n função agregada(nó, ha) {\n if(limite && ha.all.length === limite) {\n return\n }\n\n if (node.value && node.value.length) {\n if(!limit || (ha.all.length + node.value.length) < limite) {\ n ha.addAll(node.value);\n } else {\n // O limite é menor que o número de entradas no node.value + ha combinados\n ha.addAll(node.value.slice(0, limit - ha.all.length))\n return\n }\n }\n\n for (var k in node) {\n if (limit && ha.all. comprimento === limite){\n return\n }\n if (k != 'valor') {\n agregado(nó[k], ha);\n }\n }\n }\n }, \n get: função (frases, redutor, limite) {\n var self = this,\n haKeyFields = this.options.indexField ? [this.options.indexField] : this.keyFields,\n ret = undefined,\n accumulator = undefined;\n\n if (reducer && !this.options.idFieldOrFunction) {\n throw new Error('To use the acumulador, você deve especificar e idFieldOrFunction');\n }\n\n frases = (frases instanceof Array) ? frases : [frases];\n\n for (var i = 0, l = frases.comprimento; i < l; i++)\n {\n var correspondências = this._get(frases[i], limite);\ n\n if (redutor) {\n acumulador = redutor(acumulador, frases[i], correspondências, este);\n } else {\n ret = ret ? ret. addAll(matches) : new HashArray(haKeyFields).addAll(matches);\n }\n }\n\n if (!reducer) {\n return ret.all;\n }\n\n return accumulator;\ n },\n getId: function (item) {\n return typeof this.options.idFieldOrFunction === 'function' ? this.options.idFieldOrFunction(item) : item[this.options.idFieldOrFunction];\n }\n};\n\nTrieSearch.UNION_REDUCER = function(acumulador, frase, correspondências, trie) {\n if (acumulador == = indefinido) {\n retornar correspondências;\n }\n\n var mapa = {}, i, id;\n var maxLength = Math.max(acumulador.comprimento, correspondências.comprimento);\n var resultados = [ ];\n var l = 0;\n\n // Um ​​loop, O(N) para comprimento máximo do acumulador ou partidas.\n for (i = 0; i < maxLength; i++) {\n if (i < acumulador.comprimento) {\n id = trie.getId(acumulador[i]);\n mapa[id] = mapa[id] ? mapa[id] : 0;\n mapa[id]++; \n\n if (mapa[id] === 2) {\n resultados[l++] = acumulador[i];\n }\n }\n\n if (i < correspondências.comprimento) {\n id = trie.getId(matches[i]);\n map[id] = map[id] ? map[id] : 0;\n map[id]++;\n\n if (map[id] === 2) {\n results[l++] = matches[i];\n }\n } \n }\n\n retorna resultados;\n};\n\nmodule.exports = TrieSearch;\n\n\n//# sourceURL=webpack://VLibras/./node_modules/trie-search/src/ TrieSearch.js?")},"./node_modules/url-join/lib/url-join.js":function(module,exports,__webpack_require__){eval("var __WEBPACK_AMD_DEFINE_FACTORY__, __WEBPACK_AMD_DEFINE_RESULT__;(função (nome, contexto, definição) {\n if ( true && module.exports) module.exports = definition();\n else if (true) !(__WEBPACK_AMD_DEFINE_FACTORY__ = (definição), \\/\\/\\//)) {\n strArray[0] = strArray[0].replace(/^([^/:]+):\\/*/, '$1:/// ');\n } else {\n strArray[0] = strArray[0].replace(/^([^/:]+):\\/*/, '$1://');\n } \n\n for (var i = 0; i < strArray.length; i++) {\n var component = strArray[i];\n\n if (typeof component !== 'string') {\n throw new TypeError('Url deve ser uma string. Recebido ' + componente);\n }\n\n if (componente === '') { continue; }\n\n if (i > 0) {\n // Removendo as barras iniciais para cada componente, exceto o primeiro.\n component = component.replace(/^[\\/]+/, '');\ n }\n if (i < strArray.length - 1) {\n // Removendo as barras finais para cada componente, exceto o último.\n component = component.replace(/[\\/]+$/, '' ); \n } else {\n // Para o último componente, combinaremos várias barras em uma única.\n component = component.replace(/[\\/]+$/, '/');\n }\ n\n resultArray.push(component);\n\n }\n\n var str = resultArray.join('/');\n // Cada componente de entrada agora é separado por uma única barra, exceto o possível primeiro plano parte do protocolo.\n\n // remove a barra antes dos parâmetros ou hash\n str = str.replace(/\\/(\\?|&|#[^!])/g, '$1');\ n\n // substituir? em parâmetros com &\n var parts = str.split('?');\n str = parts.shift() + (parts.length > 0 ? '?': '') + parts.join('&' );\n\n return str;\n }\n\n return function () {\n var input;\n\n if (tipo de argumentos[0] === 'objeto') {\n input = argumentos [0];options.playWellcome && (() => this.player.playWellcome()),\n targetPath: options.rootPath ? url(options.rootPath, "/target") : "target",\n personalização: options.personalization,\n opacity: options.opacity,\n wrapper: options.wrapper,\n });\n\n this .opacity = options.opacity;\n this.wrapper = options.wrapper;\n\n if (this.opacity) {\n if (\n this.opacity == 0,0 ||\n this.opacity == 0,25 ||\n this.opacity == 0,5 ||\n this.opacity == 0,75 ||\n this.opacity == 1,0\n ) {\n this.opacity = parseFloat(this.opacity);\n } else {\n this.opacity = 0.0;\n }\n } else {\n this.opacity = 0.0;\n }\n\n this.rootPath = options.rootPath;\n this.personalization = options.personalization ;\n this.element = document.querySelector("[vp]");\n\n this. \n};\n\nPlugin.prototype.buildAbsolutePath = função (caminho) {\n return this.rootPath ? this.rootPath + "/" + path : path;\n};\n\nPlugin.prototype.loadImages = function () {\n this.element.querySelectorAll("img[data-src]").forEach(( image) => {\n const imagePath = image.attributes["data-src"].value;\n image.src = this.buildAbsolutePath(imagePath);\n });\n};\n\nPlugin. protótipo.loadFont = function () {\n const fontPath = this.buildAbsolutePath("assets/OpenSans-Semibold.ttf");\n const font = new FontFace("Open Sans", "url(" + fontPath + ") ");\n\n fonte\n .load()\n .then((carregado) => {\n document.fonts.add(carregado);\n })\n .catch((erro) => {\n console.error("Erro ao carregar fonte:", erro);\n });\n};\n\nmodule.exports = Plugin; \n visibilidade = false; \n\n });\n speed3.addEventListener('click', () => {\n this.setSpeed(2.0, 'x3', elem_speed, speed_default, img, border);\n visibilidade = false; \ n\n });\n\n\n};\n\n\nControls.prototype.setSpeed ​​= function (speed, label, elem_speed, speed_default, img, border) {\n img.style.display = 'nenhum ';\n border.style.display = 'nenhum';\n speed_default.style.display = 'bloco';\n speed_default.style.color = 'cinza';\n speed_default.style.border = '1px cinza sólido ';\n speed_default.style.borderRadius = '3px 3px 3px 3px';\n speed_default.innerHTML = label;\n speed_default.style.padding = '3px 4px';\n speed_default.style.fontSize = '15px'; \n elem_speed.style.display = \"none\";\n\n this.player. \\n </div>\\n <div id=\\"vpw-info-meta\\">\\n <p>Versão 5.2.0</p>\\n <a href=\ \"http://vlibras.gov.br\\" target=\\"_blank\\">vlibras.gov.br</a>\\n </div>\\n <div id=\\" vpw-info-text\\">\\n <p>\\n O VLibras é uma ferramenta gratuita (de código aberto e distribuição\\n livre) que faz a tradução automática tica da Língua Portuguesa para a Língua Brasileira de Sinais (Libras). Pode ser instalado em qualquer site ou no\\n computador, e também em tablets e smartphones.\\n </p>\\n <p>\\n Resultado de uma parceria entre a Secretaria de Governo Digital do\ \n Ministério da Economia e a Universidade Federal da Paraíba (UFPB), \n this.element.classList.remove('active');\n\n MessageBox.LEVELS.forEach(function(level) {\n this.element.classList.remove(level);\n }, this); \n};\n\nMessageBox.prototype.show = function(level, message, time) {\n var self = this;\n\n level = MessageBox.LEVELS.indexOf(level) == -1 ? 'info' : level;\n\n this.hide();\n\n self.element.classList.add('active');\n self.element.classList.add(level);\n self. element.querySelector('.vpw-message').innerHTML = message;\n\n self.message = {\n text: message\n };\n\n var ref = self.message;\n if (time ) {\n setTimeout(function () {\n self.hide(ref);\n }, time + 1);\n }\n\n return this.message;\n};\n\nmodule.exports = MessageBox;\n\n\n//# sourceURL=webpack://VLibras/./plugin/components/MessageBox/index.js?")},". function() {\n\n if(opacidade < 1.0){\n opacidade+=0.25\n } else {\n opacidade=1.0\n }\n\n if (opacidade > 1) {\n opacidade=1.0\ n }\n\n this.percent.innerHTML = opacity*100 + '%';\n window.dispatchEvent(new CustomEvent('vw-change-opacity', {detail: 1-opacity})); \n\n }.bind(this));\n\n\n this.element.querySelector('.vpw-content > ul .vpw-about')\n .addEventListener('click', function() { \n this.hide(false);\n this.infoScreen.show();\n }.bind(this));\n\n\n // Botão Sobre\n this.element.querySelector('.vpw -content > ul .vpw-about')\n .addEventListener('click', function() {\n this.hide(false);\n this.infoScreen.show();\n }.bind(this) );\n\n\n // Nacional\n this.national = this.localism.querySelector('.índice + 1)\n .lastIndexOf(" ");\n const lastIndexOfSpace = sentença\n .substring(índice + 1, sentença.comprimento)\n .indexOf(" ");\n const firstSpaceAfterSelection =\n lastIndexOfSpace = == -1 ? sentença.comprimento : índice + 1 + lastIndexOfSpace;\n\n return {\n wordToSuggest: sentença.substring(\n lastSpaceBeforeSelection + 1,\n firstSpaceAfterSelection\n ),\n begin: lastSpaceBeforeSelection + 1,\n newEnd: firstSpaceAfterSelection ,\n };\n};\n\nconst replaceByStartAndEnd = (string, substituição, início, fim) => {\n return `${string.substring(0, início)}${substituição}${string. substring(\n fim,\n string.length\n )}`;\n};\n\nSuggestionScreen.prototype.load = function (element) {\n this.element = element;\n this.element.innerHTML = template;\n\n this.rate = null;\n this.textElement = this. classList.remove(\"left\");\n\t\t\t\tdocument.querySelector('[vw-access-button]').style.margin = \"0px 0px 0px -100px\"; \n\t\t\t}\n\t\t});\n\n\t\twindow.addEventListener('vp-widget-close', (evento) => {\n\t\t\ taccess.classList.toggle('active');\n\t\t\twrapper.classList.toggle('active');\n\n\t\t\tdocument.body.removeChild(document.querySelector('. vw-links'))\n\n\t\t\tvar tagsTexts = document.querySelectorAll('.vw-text');\n\t\t\tfor (var i = 0; i < tagsTexts.length; i++) {\n\t\t\t\tvar parent = tagsTexts[i].parentNode;\n\t\t\t\tparent.innerHTML = tagsTexts[i].innerHTML;\n\t\t\t }\t\n });\n\n\twindow.addEventListener('vw-change-opacity', (evento) => {\n\t\twrapper.style.background = `rgba(235,235,235, ${event .detail})`;\n\t\t//wrapper.setAttribute( 'estilo', \n var docEl = document.documentElement;\n\n var scrollTop = window.pageYOffset || docEl.scrollTop || body.scrollTop;\n var scrollLeft = window.pageXOffset || docEl.scrollLeft || body.scrollLeft;\n\n var clientTop = docEl.clientTop || body.clientTop || 0;\n var clienteLeft = docEl.clientLeft || corpo.clienteEsquerda || 0;\n\n var top = box.top + scrollTop - clientTop;\n var left = box.left + scrollLeft - clientLeft;\n\n return { y: Math.round(top), x: Math.round (à esquerda) };\n return {\nx: xPos,\ny: yPos\n };\n}\n\nfunction createAccessLinkBox(){\n var template = __webpack_require__(/*! ./tooltip.html */ \"./widget/src/components/AccessButton/tooltip.html\").default; \n let div = document.createElement('div');\n div.className = 'vw-links';\n div.innerHTML = template;\n document.body.